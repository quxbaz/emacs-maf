#+title: maf concepts
#+HTML_HEAD: <link rel="stylesheet" href="style.css" />

Architectural overview of the maf codebase. Read this before diving into
any specific file.

* The flow

A maf command runs in four phases:

1. *Macro expansion (load time)* — =maf-defcmd= parses the declaration and
   generates a =defun= that wires the other three phases together.
2. *Resolve (call time)* — given =point= and the calc stack state, produce
   a /context alist/ describing what the command should operate on and how
   to commit the result.
3. *Body (call time)* — the user's code. Receives clean values via
   bindings, computes a result, calls =commit=.
4. *Commit (call time)* — applies the body's result to the calc buffer
   per the context's instructions.

#+begin_example
maf-defcmd declaration                        (load time)
    │
    ▼
macro expansion → defun
    │
    ▼ (call time)
resolve → context alist ──┐
    │                     │
    ▼                     │
body (sees bindings)      │
    │                     │
    ▼  val                ▼
commit ─── reads context, applies val
#+end_example

Resolve and commit are peer concerns: resolve reads state into the
context, commit writes state from the context. They share the context
schema documented at the top of [[file:../../core/maf-resolve.el][maf-resolve.el]].

* The target system

Resolve dispatches to one of six /targets/ based on where point is and
what calc state is active. In priority order:

| Target    | When it fires                                              | Operates on                         |
|-----------+------------------------------------------------------------+-------------------------------------|
| region    | An active Emacs region lies within one entry               | The run of chain terms (or the sub-formula) the region covers |
| selection | An active calc selection exists anywhere on the stack       | The selected sub-formula            |
| home      | Point is past the last stack entry (at or below =.=)        | The top stack entry                 |
| subexpr   | Point is inside an entry's formula text                     | The sub-formula at cursor           |
| equation  | Point is on a stack entry that is a relation (=, !=, ...)   | LHS and RHS (body runs once per side)           |
| entry     | Point is at line-prefix or EOL of a stack entry             | The whole formula                   |

The cascade lives in =maf--resolve-context=. The predicates live in
[[file:../../core/maf-lib.el][maf-lib.el]] (=maf--at-*-p=) and [[file:../../core/maf-sel.el][maf-sel.el]] (=maf--sel-any-p=); region
is just =use-region-p= — the most deliberate gesture there is, so it
outranks even an explicit calc selection.

Each target has a dedicated =maf--resolve-target-X= function that
produces the target-specific portion of the context alist.

The equation target also fires by /conversion/: when home, entry, or
subexpr resolves an =:expr= that is itself a relation, the context is
rewritten to equation after the fact (see [[*Relation mapping][Relation mapping]]).

The region target frequently resolves /as/ another target: whenever the
region denotes something a plain subexpr could express, it collapses
into a subexpr context and only the pointing gesture was new (see
[[*The region target][The region target]]).

** Per-command targeting policy

Every conventional defcmd generates a =NAME-targets= variable — the
narrowing gestures (region, selection, subexpr) the command currently
honors — consulted on each run and passed to resolve as =:narrowing=,
where it gates the cascade's three narrowing branches. The user
retunes a command by editing the list; a gesture off it is treated as
absent, skipped rather than consumed (commit reads =:narrowing= too,
so a suppressed selection never pulls the pushed result into itself).
The default value encodes the declared =:scope=, so shipped behavior
is the variable's initial state.

The value is checked against the command's /capability/ — =:targets=
in the declaration, defaulting to all three, or to none for
=:scope entry= commands — and a symbol outside it (including a typo)
signals when the command runs. An empty capability generates no
variable at all: bespoke-targeting commands (=:targets custom=, the
entry-scoped workers) are simply not part of this surface. Workers
name their variable after the public command with =:targets-var=
(the two substitution workers share =mafcmd-substitute-targets=).

Across a flag dispatch the pressed key's policy governs, whichever
variant answers: the invoking command sends its policy along raw, and
the dispatcher validates it after consuming the I/H flags — a
misconfigured variable must not strand a flag for the next command —
then the variant prefers it (=maf--dispatch-narrowing=) over its own
variable. =I L= maps by ln's variable though exp runs, =H E= by exp's
though exp10 does, =I |= by vconcat's though vconcatrev does. A
variant's own variable decides only its direct invocation.

** Whole-entry scope

=:scope entry= in a command's options bypasses the cascade above: the
sub-formula targets (region, selection, subexpr) never fire, and the
command resolves =entry= at point — or =home= when point is past the
last entry — however the pointing gesture looked. Point still chooses
/which/ entry; it just cannot narrow the subject inside it.

For commands with no sub-formula meaning: building a relation between
two entries (=mafcmd-equal-to=), removing one (=mafcmd-remove-equal=),
solving (=mafcmd-auto-solve=, =mafcmd-solve-for=), finding roots
(=mafcmd-poly-roots=).
Without it, the far more common gesture — point resting somewhere
inside the formula — would resolve to whatever sub-formula happens to
sit under the cursor and the command would act on that instead.

Commit honors the same intent: an entry-scoped push binds
=calc-use-selections= to nil so calc does not redirect the new value
into an active sub-formula selection, and the replacement entry
carries no selection.

A caller can still put sub-formula targeting back on top of an
entry-scoped worker, without resolve knowing anything about it: read
point yourself before invoking the worker and pass what you found in a
dynamic variable. =mafcmd-isolate= does exactly that — it hands
=maf--solve-target= the sub-expression under point, which the worker
solves the entry for — while =mafcmd-auto-solve= runs the same worker
without looking at point at all. Two commands, one solver, and the
targeting difference is the whole of what separates them.

** Entry-scoped commit

=:commit-scope entry= is the other half of the same idea, and
independent of =:scope=: where =:scope= decides what the body
/receives/, =:commit-scope= decides where its value /lands/. With it,
the value replaces the whole entry at the target's stack level instead
of being spliced back into the sub-formula slot the target named. The
cascade still runs in full — point narrows to a sub-formula, a region,
a selection, as ever — and what changes is that the part it picked out
stands as the entry afterwards, the formula around it discarded.

=mafcmd-raise= is the whole use: resolve a part, keep only it. Its
body is one call, =(commit expr)=, and every bit of the behavior is in
where that value goes.

Such a push needs the =calc-use-selections= bypass above for the
opposite reason an entry-scoped command does: its target may well /be/
a selection, and letting calc redirect the push into that selection
would splice the value back into the very formula the command exists
to throw away.

* The region target

Calc stores a sum as a left-nested binary tree and a product as a
right-nested one — =a + b + c= is =((a + b) + c)=. A contiguous run of
terms like =b + c= is therefore not a node, and calc's manual declares
it unselectable ("there is no way to select the =b - c + d= portion").
The region target fills exactly that gap: an active region resolves to
the run of chain terms it covers, and every defcmd works on it with no
per-command code.

The region is never parsed as text. Its two endpoints map into the
entry's flat rendering ([[file:../../core/maf-comp.el][core/maf-comp.el]]), and the innermost
sub-formula containing both — the /container/ — decides what the
gesture means:

- *Not a chain* (=+=/=-= or =*=) → resolve as subexpr on the container
  itself: the smallest sub-formula defined by two points, the same
  semantics as calc's =j a=.
- *A chain* → snap the region outward to the run of terms it touches.
  Operator glyphs are connective tissue: a trailing =+= is absorbed,
  and a region covering /only/ an operator pulls in both neighbors.
  Then:
  - run = the whole chain → subexpr on the container;
  - run = one term → subexpr on that term;
  - anything else → a synthesized *region* context.

Only the synthesized case is new machinery; note that a left-anchored
sum run (=a + b= in =a + b + c=) or right-anchored product run /is/ a
real subtree and never reaches it.

** Runs and signs

[[file:../../core/maf-chain.el][core/maf-chain.el]] views a chain as its rendered term list — each term
paired with the operator glyph in front of it. Only the spine is
flattened (left for sums, right for products, matching calc's reader);
an off-spine sub-chain renders parenthesized, reads as a unit, and
stays one term.

A run's =:expr= is the fold of its covered terms, /each keeping its
chain sign/, so the fold is exactly the value the run contributes to
the chain. Selecting =4 x - 12= in =a - 4 x - 12= yields
=-(4 x) - 12= — including the leading term's sign, whose glyph sits
just outside the region — and that is what keeps the splice
value-preserving: the sign is structural, not textual.

** Region commit

A synthesized run has no cons to =eq=-splice, but its container does:
commit rebuilds the chain — the untouched terms keep their original
conses, so their text re-renders identically — with the body's result
joining as one =+= (or =*=) term, then splices the rebuilt chain at
=:chain-ref= through the ordinary eq-based path. A result that is
itself a same-kind chain is absorbed as /terms/ rather than spliced as
a parenthesized unit (commuting =a b= in =a b c= gives =b a c=, not
=(b a) c=); the absorption only reassociates.

Resolving consumes the gesture — the mark deactivates — while a
resolve error leaves the region active for adjustment. Version-1 scope
is =+=/=-= and =*= chains; =/= is excluded because its textual and
structural readings diverge (=b / c= inside =a / b / c= contributes
=1/(b c)=, not =b/c=).

* The context alist

Everything resolve learns is carried in one flat alist — the /context/
— which the later phases read but never mutate. =maf--resolve-context=
assembles it from three layers, appended in order, so earlier keys
shadow later ones:

1. *Target keys* — from the matched =maf--resolve-target-X= function,
   possibly converted by relation mapping (below).
2. *Declaration options* — the defcmd's OPTS merged in verbatim. Known
   keys are =:arity= (the symbol =unary= or =binary=), =:prefix= (the
   trail label string), =:map= (=-1= opts out of relation mapping),
   =:pair= (=-1= keeps a relation argument whole at an equation target),
   =:commit-scope= (=entry= lands the value as the whole entry — see
   [[*Entry-scoped commit][Entry-scoped commit]]), and the I/H variant links (command symbols);
   any other keyword
   passes through untouched, so resolve/commit extensions can define
   their own.
3. *Ambient state* — snapshots taken at resolve time: =:keep= (the
   =calc-keep-args-flag= boolean) and =:point= (an alist of point's
   placement, captured before the rewrite destroys it — see
   [[*Point restoration][Point restoration]] for its keys).

For example, a binary mult command (=:prefix "mult"=) invoked with
point on the =x= of =2: 6 x + 12=, with =5= at the top of the stack,
resolves to:

#+begin_example
((:target    . subexpr)                ; target keys
 (:expr      . (var x var-x))
 (:expr-ref  . (var x var-x))          ; the encased cons from the entry
 (:arg       . 5)
 (:m         . 2)
 (:commit-m  . 2)
 (:commit-n  . 1)
 (:post-pop  . 1)
 (:reselect  . nil)
 (:arity     . binary)                 ; declaration options
 (:prefix    . "mult")
 (:keep      . nil)                    ; ambient state
 (:point     . ((:pos . 7) (:line . 1) (:affinity . nil))))
#+end_example

Two consumers read it. The macro-generated command wires the body:

- =:target= — a symbol: =region=, =home=, =selection=, =subexpr=,
  =equation=, or =entry=. Selects the execution shape (single body run
  vs once per side); commit also dispatches on it to pick the splice
  path. The macro also binds it to the dynamic variable =maf-target=
  around the body, for bodies whose /result shape/ depends on where it
  lands (see [[*Multi-value commit][Multi-value commit]]); most bodies
  ignore it and just act on EXPR.
- =:expr= — the clean operand, a normalized calc formula, bound as the
  body's EXPR.
- =:arg= — the second operand for binary commands, a normalized calc
  formula, bound as ARG; nil for unary.
- =:rel-op=, =:lhs=, =:rhs= — equation target only. =:rel-op= is the
  relation's head symbol (=calcFunc-eq=, =calcFunc-neq=, =calcFunc-lt=,
  =calcFunc-leq=, =calcFunc-gt=, or =calcFunc-geq=); =:lhs= and =:rhs=
  are the two sides as normalized formulas. The body runs once with
  EXPR bound to =:lhs=, once to =:rhs=, and the two results are
  reassembled under =:rel-op= before a single commit.

=maf--commit= applies the result:

- =:commit-m=, =:commit-n=, =:post-pop= — integers encoding the
  push/pop recipe: the stack level to push at, the entries popped
  there first, and the entries popped from the top afterward (see
  [[*The commit recipe][The commit recipe]]).
- =:m= — integer stack level (1 = top) of the target entry; where
  commit fetches the full formula for splicing.
- =:expr-ref= — the encased cons cell for the eq-based splice.
  Selection and subexpr only (see [[*Encasing][Encasing]]).
- =:chain-ref=, =:chain-kind=, =:pre-terms=, =:post-terms= — region
  target only: the chain cons commit splices the rebuilt chain at, its
  kind (=sum= or =prod=), and the untouched terms on each side of the
  run (see [[*The region target][The region target]]).
- =:reselect= — boolean: whether the committed result becomes the new
  selection.
- =:prefix= — string: trail label recorded with the operation.

The commit instructions are computed /at resolve time/, not by commit:
each target function knows how its own result should land (including
the keep-args variants), and commit just executes the recipe. That
split is what keeps commit target-agnostic apart from the splicing
special case.

The authoritative key-by-key schema lives at the top of
[[file:../../core/maf-resolve.el][maf-resolve.el]].

** Point restoration

Calc's command epilogue parks point at home after every command. The
=:point= snapshot is what undoes that: it is consumed /after/ commit,
as the generated command's last act — restore has to run once
=calc-wrapper= returns, because anything done inside the wrapper is
clobbered by the epilogue.

The snapshot (=maf--point-snapshot=) is itself an alist of three keys:

- =:pos= — integer: point's buffer position.
- =:line= — integer: the line number at point.
- =:affinity= — a symbol classifying the position semantically: =home=
  when point was at or below the =.= line, =eol= at end of line, =bol=
  in the line-number prefix, or nil inside an entry's formula text.

=maf--point-restore= applies it:

- =home= affinity restores nothing; calc's default placement already
  matches.
- Otherwise point returns to =:pos=, corrected back to =:line= if the
  rewrite shifted it, and =eol=/=bol= affinity is re-applied on the
  line rather than the exact position.

Line-based restore is correct for every target because of a commit
invariant: commits only pop entries at the target's level and below it
in the buffer (=:post-pop= consumes the stack top, which is displayed
/below/ the target), so the target entry's line never moves.

One case restores by /anchor/ rather than position: point on a glyph
the resolved sub-formula renders itself — its operator, a comma, its
function name. Resolve records the glyph's index as =:point-anchor=,
commit returns where the committed node landed (its cons and stack
level), and point goes to that node's same structural glyph, wherever
the rewrite moved it. Commuting =sin(y) = k= from the = leaves point on
the = of =k = sin(y)=, and the anchor survives the glyph itself
changing (commuting =x < y= parks point on the =>=). It also holds
through relation mapping — factoring from the = re-anchors on the
rebuilt relation's = — and spaces count as glyphs, since a juxtaposed
product renders its multiplication as nothing but a space: commuting
=2 (4 + 3 x)= from that space lands after the =)= of =(4 + 3 x) 2=.
Atoms have no anchor, and anything the anchor can't locate afterwards
\(non-flat renderings, a consumed entry) falls back to the positional
restore. The node-to-text
coordinate machinery lives in [[file:../../core/maf-comp.el][core/maf-comp.el]]. Sticking point to the
same /atom/ when the formula reshapes around it is deliberately
deferred.

** Relation mapping

A relation can become the subject without point sitting on a relation
entry: the top entry at home, an entry target at the margin, or the
relation node itself under point as a subexpr. =maf--resolve-map-relation=
inspects every home, entry, and subexpr context after resolution; when
its =:expr= is a relation, it prepends the equation keys — =:target=,
=:rel-op=, =:lhs=, =:rhs= — onto the context. The base target's commit
fields remain (only =:target= is shadowed), so the rebuilt relation
replaces exactly what the base target would have replaced.

Commands whose body consumes or produces relations whole (solve,
mapeq, the relation builders) opt out with =:map -1=, which also
disables the direct equation branch of the cascade. So do the
concatenators (the =|= family), for a different reason: they take a
relation as one /element/ of the vector they build, so two stacked
equations give =[x = 1, y = 2]= — calc's own spelling of a system —
where mapping would pair them into the single equation =[x, y] = [1,
2]=. At a mapped equation
target, a relation argument ordinarily pairs its two sides with the
subject's sides — and signals unless both relations are =. =:pair -1=
keeps that argument whole without disabling per-side mapping of the
subject, and the pairing check goes with it: =mafcmd-let='s assignment
x = 2 binds across a < subject as readily as an = one.

Synthesized region contexts are exempt by construction — a run of
chain terms is never a relation. Degenerate regions resolve /as/
subexpr, though, so a region snapped to a whole relation node maps per
side like any subexpr would.

** The map flag

=mafcmd-map-flag= (=M=) sets =maf-map-flag= through calc's fancy-prefix
machinery, so it chains with =K=/=I=/=H= and expires like them
(=maf--map-flag-expire= sweeps it after a command that does not read
it; keystrokes in a minibuffer never spend it, so it survives a
prompting command's own prompt through to the defcmd worker). The next
defcmd consumes the flag at resolve time: a relation subject takes the
equation target even past =:map -1= — the flag is the explicit request
to map, so =M |= on two stacked equations /does/ pair them into
=[a, c] = [b, d]=, and a selected relation, ordinarily held whole as a
deliberate gesture, converts too, though a forced =:map -1= command
splits only an = (=maf--resolve-mapflag-relation-check=: an ordered
relation or != refuses, since a command cannot say which way it bends
the direction, and =I= already means the inverse variant) — and a
vector subject runs the body
once per element (=maf--defcmd-map-vec=; nested vectors recurse, the
arg of a binary command shared across the runs). Any other subject
refuses. Where =M M= maps a formula you type and =M $= one from the
stack (=maf--map-flag-keys=, live for the keypress after =M=), =M=
maps a command, with no operator table behind it the way
calc's =V M= has.

* Encasing

Calc's mechanism for distinguishing duplicate atoms by =eq=.

Consider =1 + 1= = =(+ 1 1)= in calc. The two =1=s are the same integer
object — both =eq= and =equal=. If the user selects "the first one", you
can't tell them apart structurally.

=calc-encase-atoms= (in calc's =calc-sel.el=) walks the formula and replaces
each naked atom with =(cplx atom 0)= — a fresh cons cell each time:

#+begin_example
(+ 1 1)    →    (+ (cplx 1 0) (cplx 1 0))
                ;; ^^^^^^^^^^  ^^^^^^^^^^
                ;; cons #A     cons #B   ← now distinguishable by eq
#+end_example

Calc displays =(cplx N 0)= as just =N= (complex with zero imag), so the
user sees nothing different.

=calc-prepare-selection= calls this on a stack entry and *mutates the
entry in place* via =setcar=. So once selection-related machinery runs
on an entry, that entry's atoms are encased until something normalizes.

This matters for us in two places:

1. *The body shouldn't see encased values.* Most =calcFunc-*= functions
   dispatch on type, and =(cplx ...)= inputs route to complex-number
   branches that produce floats. (E.g., =calcFunc-sqrt= on =(cplx 16 0)=
   gives a float instead of the integer 4.) So resolve exposes a /clean/
   =:expr= via =math-normalize=.

2. *Commit's splice is =eq=-based.* =calc-replace-sub-formula= matches by
   =eq=, and only the original encased cons matches the cell in the
   entry's formula. So resolve also exposes =:expr-ref= — the actual
   cons cell — for commit to use.

* :expr vs :expr-ref

The body and commit operate on the same sub-formula but at different
points in the data flow:

- =:expr= — clean form, for the body to compute with
- =:expr-ref= — encased cons, for commit's =eq=-based splice

They're only different for *selection* and *subexpr* (the targets that
splice into a parent formula). =home= and =entry= don't have =:expr-ref=
— they operate on whole entries, no sub-formula targeting needed.

A synthesized *region* run has an =:expr= but no =:expr-ref= at all:
the run is not a cons in the entry, so there is nothing to =eq=-match.
Commit instead rebuilds the containing chain and splices it at
=:chain-ref= — which /is/ a real node — through the same eq-based path.

* Reselect

When commit pushes the new entry, should the result be re-selected?

- *selection* target: yes (=:reselect t=). User explicitly set a
  selection; preserve their intent by re-selecting the new sub-formula.
- *subexpr* target: no (=:reselect nil=). User just had a cursor
  position; no explicit selection to preserve.
- *region* target: no (=:reselect nil=). The gesture is transient —
  the mark deactivates at resolve — and there is no prior calc
  selection to preserve.
- *home / entry / equation*: not applicable (no sub-formula selection).

Commit reads =:reselect= and passes =val-encased= (or nil) as the SELS
arg to =calc-pop-push-record-list=.

* Keep-args

=calc-keep-args-flag= is calc's mode where operations don't consume
their inputs — the result is pushed /above/ them rather than replacing
them.

Resolve snapshots the flag as =:keep= and derives commit instructions
accordingly:

| Target    | Normal mode                                       | Keep-args mode                    |
|-----------+---------------------------------------------------+-----------------------------------|
| home      | Pop arity entries, push result at top             | Push result at top, no pop        |
| selection | Replace selected entry in place                   | Push result on top, leave entry   |
| subexpr   | Replace entry in place (sub-formula spliced)      | Push spliced result on top        |
| region    | Replace entry in place (chain rebuilt)            | Push rebuilt result on top        |
| entry     | Replace entry in place                            | Push result on top                |

In schema terms, =:commit-m=, =:commit-n=, and =:post-pop= encode the
recipe; commit just executes it.

A hand-written command may read =:keep= for something else. =maf-dup=
(RET) does: a duplicate consumes nothing to begin with, so the flag
cannot change what lands on the stack, and it spends it on point
instead — =K RET= duplicates and holds point where it was, exactly as
=C-u RET= does. RET only sees the flag at all through
=maf--fancy-prefix-keep=; calc's fancy-prefix dispatch clears its flags
before any control character, and before any function key too.

That guard reads the =maf-command= property. =maf-defcmd= stamps it on
every command it defines, since their commit path reads =:keep=; a
hand-written command that wants the same treatment sets it itself, as
=maf-dup-or-clear-selections= does. A key with no marked command behind
it keeps calc's own behavior, which matters because the flag is not
inert — =maf-dup= branches on it.

* The commit recipe

Three context keys describe what commit should do:

- *=:commit-m=* — stack level to push at (the M arg to
  =calc-pop-push-record-list=).
- *=:commit-n=* — entries to pop at =:commit-m= before pushing (the N arg).
  This is the "replace in place" knob — 1 means "pop the old, push the
  new", 0 means "insert on top of it."
- *=:post-pop=* — entries to pop /from the top/ /after/ the push.
  This consumes the binary arg in non-keep mode.

Visualization for binary selection at =:m = 2= (non-keep):

#+begin_example
Before:                              commit-m=2, commit-n=1, post-pop=1
1: arg                ──┐
2: selected entry  ──┐  │
                     │  │
                     ▼  ▼
After push at m=2 (pop 1 at level 2, push new):
1: arg
2: new (with spliced sub-formula)
                     │
                     ▼
After post-pop 1 from top:
1: new
#+end_example

Two operations, one push and one optional post-pop. See =maf--commit-push=.

* Multi-value commit

A body normally commits one expression. At a /whole-entry/ target
(=home=, =entry=) it may instead commit a *list* of values, which lands
as one stack entry per value: =calc-pop-push-record-list= already
distinguishes the two, reading its VALS as a list of values when the
car is an integer or a cons, and as a single value otherwise. A calc
formula always leads with a symbol head (=+=, =calcFunc-sin=, =vec=),
so the two never collide.

A /slot/ target (=selection=, =subexpr=, =region=) cannot take a list:
its result is spliced into one cons in a larger formula, so exactly one
expression fits. Committing a list there signals.

Bodies that can produce either read =maf-target= to tell which applies:

#+begin_example
(cond ((eq maf-target 'equation)                ; forced per-side run
       (if (cdr parts) expr (car parts)))       ; a side's slot fits one
      (t parts))                                ; one entry per part
#+end_example

=mafcmd-unpack= is the case in point — =[x, y]= becomes two entries.
Entry-scoped as it is, the only slot it can meet is a relation's side
when the map flag forces a per-side run, and that slot fits a single
part.

* Widening the sub-formula target

A slot fits one expression, so a command whose result does not fit the
node under point would simply do nothing there — the key would look
broken from most positions in a formula. =:widen= is the way out: the
declaration names a predicate, and resolve walks from the node under
point outward through its ancestors, stopping at the innermost one the
predicate accepts. That node becomes =:expr= and =:expr-ref=. Nothing
accepted out to the whole entry means the node under point stands, and
the command no-ops as before.

The walk starts /at/ the node under point, so a position that already
had an acceptable target is unaffected — widening only ever fills in
where the command used to be silent.

For =mafcmd-flatten= the predicate is "a vector with nesting to
remove", so the command means *flatten the innermost nested vector
around point*:

#+begin_example
[[1|, 2], [3, 4]]      =>  [1, 2, 3, 4]      ; point names the flat row
                                             ; [1, 2]; the matrix holding
                                             ; it has the nesting
x + [[1, 2], [3, 4]]|  =>  x + [1, 2, 3, 4]  ; already acceptable: no walk
x + [1, 2]|            =>  unchanged         ; nothing nested out to the
                                             ; entry
#+end_example

Explicit calc selections are never widened — an active selection is a
deliberate gesture, and the selection target does not route through
=maf--resolve-widen=.

The user reaches the same walk by hand: =maf-up-expression= (C-M-u)
moves point out to the enclosing sub-formula over the same ancestors
(=maf--resolve-ancestors=), so where =:widen= fixes one command's reach
from a declaration, the motion re-aims every command by putting point
where it names the parent. It lands only where resolve agrees — an
ancestor the rendering gives point no way to name, such as a matrix
row, is walked past rather than landed on — and it carries an active
selection along with it, that being what a command would resolve to
instead of point.

** Widening a rewrite marker

=maf-distribute= (j D) and =maf-merge= (j M) widen along the same
ancestors, but for a different reason, and so outside the =:widen=
declaration. They are not defcmds: calc's =DistribRules= and
=MergeRules= are written against a marked sub-formula, and every rule
in them matches the formula /around/ the marker rather than the marker
itself —

#+begin_example
x * select(a + b)   :=  x*select(a) + x*b
#+end_example

— so the rewrite needs the whole entry with the target marked inside
it, not an isolated sub-expression spliced back into a slot. The node
point names is therefore usually one level too deep to be the marker:
on the =a= of =x (a + b)=, the rule that fires is the one reading the
product, whose marked part is the sum.

A marker is therefore always a /part/ of the formula being rewritten,
never that formula itself, and the node under point can miss in either
direction: it may be too deep to be the marker (the =a= above), or it
may already be the formula to rewrite, with the marker one level in —
point on the =ln= of =ln(x^2)=.

So =maf--sel-rewrite-widen= walks rewrite *sites*, from the node under
point out through its ancestors, trying each site's own parts as the
marker. The node under point is itself the first site, which is what
lets point stand on a function name or an operator; it comes up again
as a candidate marker when the walk reaches its parent, so standing on
a term still marks that term. Innermost site wins, and within a site
the part point is in. The upshot is that every column of an entry
reaches the same rewrite:

#+begin_example
ln|(x^2)  ln(x|^2)  ln(x^2|)   =>  2 ln(x)      ; all of them
x|*(a+b)  x*|(a+b)  x*(a|+b)   =>  x b + x a    ; all of them
#+end_example

Where =:widen= takes the innermost ancestor a declared predicate
accepts, this takes the innermost site at which /some rule matches/.
The test is the rewrite itself rather than a predicate, since "does a
rule reach this" is not a property of the node alone. Nothing matching
out to the whole entry means the entry is left untouched.

At home, where point names no part, the same walk runs over the whole
entry outermost-first (=maf--sel-rewrite-entry=). Calc's own reading of
an unmarked entry — let =select( )= match wherever it can — is not
usable, for two reasons that pull in opposite directions:

- It matches *too much*. The sign rules below match every expression
  there is, so a bare =sqrt(x)= rewrites to =sqrt(-1) sqrt(-x)=, with
  no marker relationship to test and so nothing for the sign guard to
  catch.
- It reaches *too little*. The rewriter normalizes its input, which can
  fold away the very thing a rule was about to match: =x^a x^b= becomes
  =x^(a + b)= before MergeRules ever sees it, and =j M= at home did
  nothing on an entry it plainly should merge.

Walking marked candidates fixes both, and puts home on the same footing
as every other position — one code path, sign guard included.

One family of rules has to be held off for the walk to get anywhere.
Two DistribRules mark a bare negation (=select(- a) ^ x=,
=sqrt(select(- a))=), and calc's matcher reads *any* expression as
=-a=, binding =a= to its negation — =-(-a)= is =a=. So they match every
marker there is, and sitting early in the set they claim the first
level the walk tries, before the rule the gesture meant:

#+begin_example
ln(x|^2)   =>  ln((-1)^2 (-x)^2)   ; the sign rule, equal and useless
ln(x|^2)   =>  2 ln(x)             ; the log-of-a-power rule, one out
#+end_example

Both rules carry the marker from =n= to =-n=, so =maf--sel-sign-peel-p=
counts a result marking exactly =math-neg= of what went in as no rule
having fired, and the walk carries on outward. The test is the sign of
the marked node itself, not the marker relationship, so a genuinely
negative operand still splits (=sqrt(-x)= => =sqrt(-1) sqrt(x)=). Both
nodes compared are read back out of whole expressions the rewriter saw,
never off the stack entry — an entry's atoms are encased (see
[[*Encasing][Encasing]]), and =(cplx 4 0)= would never match a plain =4=.

The same rule holds as above: an active selection is never widened, so
=j D= on a hand-made selection is faithful to =calc-sel-distribute=,
which marks exactly what was selected. It is also what picks the
*entry*, ahead of point, as in =maf--resolve-context=: a selection
standing on a lower entry is the subject even when point has wandered
to another.

*** Scoping the rewrite to the site

Only the site is handed to the rewriter, and the rest of the entry is
carried over untouched. Calc's =calc-rewrite-selection= normalizes the
whole entry on the way in, which reaches sites the gesture never
named — merging the logarithms of =ln(a) + ln(b) + x^p x^q= also
collapses the powers, two structural changes for one keystroke. maf's
other commands leave what they did not target alone, and these follow
them rather than upstream. Scoping costs no matches: the site *is*
where the rule matches, being the formula around the marker.

The same reasoning applies after the rewrite. There is no normalizing
pass over the finished result and no =calc-encase-atoms= (see
[[*Encasing][Encasing]]) — the rewriter already normalized the site, and re-running
either over the whole entry would undo the scoping. The coefficient
ordering is likewise confined to products carrying the marker, so
distributing the logarithm in =ln(x^2) + y 3= does not also reorder the
=y 3= the user wrote.

*** Ordering without folding

A distribution has to commit unsimplified, or calc's simplifier undoes
it — =x^b x^a= folds straight back to =x^(b + a)=. But calc's canonical
*ordering* lives in that same pass, so suppressing it also loses the
convention that a coefficient is written first, and =ln(x^2)= would
commit as =ln(x) 2=.

The ordering cannot simply be run afterwards. Both ways of asking calc
for it were measured against the raw result, and both undo the work:

| approach                 | =ln(x^2)=   | =x^(a + b)=   | =sqrt(3:4)=           |
|--------------------------+-------------+---------------+-----------------------|
| raw, simplifier off      | =ln(x) 2=   | =x^b x^a=     | =sqrt(1/4) sqrt(3)=   |
| normalize under ambient  | =2 ln(x)=   | =x^(b + a)=   | =1.73205 sqrt(0.25)=  |
| ambient, marker still in | =2 ln(x)=   | =x^(b + a)=   | =1.73205 sqrt(0.25)=  |

The marker does not protect here the way it does in a merge: there it
sits directly on an operand of the sum being folded
(=x*(3 + select(2))=), while in =x^select(b) * x^a= it is buried in an
exponent and the outer product folds around it.

*** Correcting calc's power-merge rules

Four MergeRules gather powers of unlike bases wrongly, and each loses
the value rather than merely spelling it oddly. Stock =calc-sel-merge=
gives the same answers — none of it is maf's doing.

Two divide by the wrong base, reading the numerator's base as =b= where
every sibling reads =a=:

#+begin_example
(a^x) / select(b^x)  :=  select((b / b) ^ x)     ; b/b = 1
(a^x) / select(b^y)  :=  select((b / b^(y-x)) ^ x)
#+end_example

Two more get the exponent arithmetic wrong. Gathering =a^x= and =b^y=
under a single power of =x= needs =(b^(y/x))^x = b^y=, but the rules
subtract where they must divide, yielding =b^(x(y-x))=:

#+begin_example
 (a^x) * select(b^y)  :=  select((a * b^(y-x)) ^ x)
select(a^x) / (b^y)   :=  select((a / b^(y-x)) ^ x)
#+end_example

At =a=2, b=4, x=4, y=2= the entry =a^x / b^y= is 1 and calc answers
1048576.

maf corrects all four rather than mirror a wrong answer, the same call
it makes for the fraction the negation rule mangles. The corrections
are swapped into whatever =var-MergeRules= currently holds, matched by
left-hand side and memoized (=maf--merge-rules=), rather than kept as a
second copy of the set — the pattern =maf--jump-rules= already uses, and
for the same reason: a hand-kept copy drifts the moment calc edits the
set upstream, whereas an unmatched left-hand side simply replaces
nothing.

So =maf--sel-coefficient-first= applies the one part of the ordering
that provably cannot undo a rewrite: moving numbers to the front of a
product. Numbers commute with every operand there is, matrices
included, so it is a reordering and never a change of value, and it
folds nothing. A symbolic coefficient is left where the rule put it
(=log(x^p, b)= => =log(x, b) p=); matching calc there would mean
reproducing an order that is not a simple total order over
expressions, calc's own product ordering being decided inside
=math-mul= and its simplification rules rather than by =math-beforep=.

* Binary on selection-like targets

Selection, subexpr, entry: each can support binary commands, but the
"second operand" needs to come from somewhere. Convention: the top stack
entry serves as =:arg=.

This creates a self-reference problem when the target itself is the top:

- *selection / subexpr / region* — reject. If the selected sub-formula
  is in the top entry, the arg would be the entry containing the
  sub-formula. No coherent commit semantic. Resolve errors out.
- *entry* — /ergonomic shift/. If point is at the line-margin of the top
  entry and arity is binary, =:m= is shifted from 1 to 2. The top
  becomes the arg, the entry below becomes the target. User doesn't
  have to position point on the operand whose value will change.

Choice reflects the difference between "I pointed at this exact
sub-formula" (selection/subexpr — error keeps the user honest) and "I'm
near this entry" (entry — shift is friendlier).

* Inverse / Hyperbolic variants

Calc's I and H prefix flags reroute a defcmd to a declared variant
command (the =:inverse=, =:hyperbolic=, and =:inverse-hyperbolic=
options — e.g. I on =mafcmd-ln= runs =mafcmd-exp=). Dispatch happens
before any context is resolved, and the flags are consumed first, so a
variant that itself declares variants does not dispatch again — that is
what keeps cross-linked pairs like ln ↔ exp from looping. A set flag
with no matching variant signals =user-error= and still consumes the
flags, so the next command starts clean.

* Contextual digit entry

The digit keys (and =_=, =.=, =#=, =@=) start calc's normal number
entry, but where the result lands depends on point at the first
keypress and on the key that ends the entry (=maf-digit-start= in
[[file:../../src/minibuffer.el][src/minibuffer.el]]). SPC pushes the number onto the stack exactly as
in plain calc, wherever point is. RET is the contextual commit
(=maf-digit-commit-contextual=): on a numeric leaf the entered number
replaces it; on any other sub-formula it multiplies it, number on the
left, with the product left literal so nothing distributes; on a
relation node it multiplies both sides. The entry is an edit of the
formula at point, and RET commits it, as it commits maf's text edits
(=maf-edit-commit=).

Away from a sub-formula there is nothing to edit and RET pushes too —
at home, in the line prefix, at EOL, in algebraic mode, for entries
that escape to algebraic, and for interval entry (=..=). The entry
minibuffer is calc's own (=calc-digit-map=), so the in-entry keys —
=_=, =:=, =@=, =#=, =..= — are unchanged but for the handful maf takes
there, below.

C-<return> is SPC's keep-point sibling (=maf-digit-commit-here=): the
same push, but point stays on the entry it was on instead of dropping
to the home line, so the next command still resolves there. It has no
RET counterpart, because RET keeps point already — the edit happens
where point stands.

=;= is the fraction colon (=maf-digit-colon=), so 3 ; 4 enters 3:4
without a modifier. It is an alias, not an insertion — it re-dispatches
calc's own =calcDigit-key= with the event spoofed to =:=, so calc's
colon handling (the leading 1 for a bare
=;=, mixed numbers, format validation) applies unchanged. During a
maf-edit session =;= types =:= too (=maf-edit-insert-colon=); the
literal semicolon — calc's row separator in matrix notation — moves to
=M-;= there (=maf-edit-insert-semicolon=). While an incomplete object
is on the stack — a vector, matrix, complex number, or interval being
entered — the digit-entry =;= steps aside for calc's own: it is the
row separator of matrix entry, typed from inside digit entry, and a
fraction there still goes in on =:=.

=n= and =P= commit the entry multiplied by pi (=maf-digit-pi=), so 2 n
gives 2 pi and 1:3 n a third of it, symbolic either way. Being a value
and its terminator in one key, =n= has no second key to split the two
destinations across: the product goes where a value goes from this
position — contextually on a sub-formula, as RET does, pushed
anywhere else. =e= ends the entry and equates with the
number typed (=maf-digit-equal-to=), the in-entry twin of =e= out in
the stack: on =x= the entry 5 e gives =x = 5=. It is calc's own
command-key termination, so the number becomes the command's argument
and the push folds into the command's undo group; the cost is the
e-notation the key was, which goes in through algebraic entry
(=' 1e6=) instead.

=j= is a jump, not a value: the number names a stack level and point
travels there (=maf-digit-jump=), landing at the entry's EOL — its
margin — so the next command takes the whole entry. Level 0 is home; a
level past the top of the stack lands on the top entry, as a jump past
the end of a buffer lands on its last line. Nothing is entered or
pushed, and the place point left is marked, so C-u C-SPC returns to it
(home excepted, being one key away on =maf-go-home=). An entry that is
not a whole number names no level and is refused, left standing in the
minibuffer to be corrected. The key is calc's =j= selection prefix out
in the stack, addressing a part of an entry where this one addresses a
level; the cost is the command-key termination it was, so 3 j e no
longer pushes the 3 and runs the prefix on it — push first (3 SPC) and
the prefix is itself again.

=n=, =P=, =e=, and =j= step aside where the key is calc's own: inside
a radix-prefixed entry — where a letter is a digit and which letters
count depends on the radix (=20#j= is 19), and where a number is
plainly a value, since no one writes a stack level in base 16 — and
with =maf-mode= off in the calc buffer the entry belongs to. =e=, =j=,
and RET stand aside for an incomplete object too: half a vector is no
entry to equate with, jump from, or commit contextually, so inside one
RET is calc's own terminator again, as it is with =maf-mode= off. SPC
stays calc's own everywhere — the element separator inside an
incomplete object (=[ 1 SPC 2 ]=), the plain push elsewhere. =n= and
=P= work inside an incomplete object, so a vector can be filled with
multiples of pi.

* Files

| File                    | Role                                                        |
|-------------------------+-------------------------------------------------------------|
| [[file:../../maf.el][maf.el]]                  | Package entry point, =maf-mode=, load order                 |
| [[file:../../core/maf-comp.el][core/maf-comp.el]]    | Flat-rendering coordinates: map formula nodes and point to entry text |
| [[file:../../core/maf-chain.el][core/maf-chain.el]]   | Chain term lists: flatten, fold, and rebuild =+=/=-= and =*= spines |
| [[file:../../core/maf-lib.el][core/maf-lib.el]]     | Generic predicates and utilities (find calc buffer, at-*-p, point snapshot/restore) |
| [[file:../../core/maf-sel.el][core/maf-sel.el]]     | Selection-state introspection (any-selection, effective-m)  |
| [[file:../../core/maf-resolve.el][core/maf-resolve.el]] | Context resolution + target dispatch                        |
| [[file:../../core/maf-commit.el][core/maf-commit.el]]  | Commit recipes (push/pop choreography per target)           |
| [[file:../../core/maf-defcmd.el][core/maf-defcmd.el]]  | The =maf-defcmd= macro + parsing helpers + I/H dispatch     |
| [[file:../../core/maf-module.el][core/maf-module.el]]  | Module system: register minor-mode modules, drive/reconcile against =maf-modules=, =maf-list-modules= toggle menu |
| [[file:../../src/maf-cmds.el][src/maf-cmds.el]]         | The mafcmd table: calcFunc-backed commands, keys, variant links |
| [[file:../../src/math.el][src/math.el]]             | Pure formula helpers (term walk, terms GCD)                 |
| [[file:../../src/stack.el][src/stack.el]]            | Hand-written contextual stack commands (factor-by, factor-gcd, undo/redo) |
| [[file:../../src/minibuffer.el][src/minibuffer.el]]       | Digit-entry integration: contextual digit entry, point/undo handoff |
| [[file:../../src/bindings.el][src/bindings.el]]         | Default key bindings made outside the mafcmd table          |
| [[file:../../modules/maf-hl.el][modules/maf-hl.el]]       | Highlight module: =maf-hl-mode= per-buffer, =maf-use-hl-mode= toggle |
| [[file:../../modules/maf-history.el][modules/maf-history.el]]  | History module: =maf-use-history-mode= records stack snapshots, browse in =*maf-history*= |
| [[file:../../modules/maf-persist.el][modules/maf-persist.el]]   | Persist module: =maf-persist-mode= saves/restores the stack across sessions |
| [[file:../../modules/maf-edit.el][modules/maf-edit.el]]      | Edit module: =maf-use-edit-mode= binds SPC/`/C-o/"(" to =maf-edit= in-place stack editing (=maf-edit-mode= session) |
| [[file:../../modules/maf-preview.el][modules/maf-preview.el]]   | Preview module: =maf-use-preview-mode= shows the entry at point in Big display in a floating panel (posframe child frame, else in-window) |
| [[file:../../modules/maf-formulas.el][modules/maf-formulas.el]]  | Formulas module: =maf-formulas= menu of saved formulas by category (detail + RET-insert); data in maf-formulas-data.el |
| [[file:../../debug/maf-step.el][debug/maf-step.el]]       | Step-through harness (cockpit) that drives the tests        |
| [[file:../../debug/maf-hl-verify.el][debug/maf-hl-verify.el]]  | Programmatic highlight verification (overlay state + render) |
| [[file:../../debug/maf-hl-sweep.el][debug/maf-hl-sweep.el]]   | Highlight sweep across many expression shapes               |
| [[file:../../debug/maf-dev-init.el][debug/maf-dev-init.el]]   | Init file for the dedicated =maf-dev= instance              |
| [[file:../../tests/][tests/]]                  | Step tests; a file passes when no step errors               |

Load order in =maf.el=: =conf= → =module= → =comp= → =chain= → =lib= →
=sel= → =step= → =hl-verify= → =resolve= → =commit= → =defcmd= →
=cmds= → =math= → =stack= → =bindings= → =minibuffer=, then the feature
modules (=maf-hl=, =maf-history=, =maf-persist=, =maf-edit=, =maf-preview=,
=maf-formulas=), then
=maf-modules-apply=. Each layer depends only on earlier ones; modules
load last so =maf-mode-map= exists for any key bindings they install.
(=maf-hl= also loads earlier as a dependency of the =hl-verify= debug
helper; the require is idempotent.)

* Modules

Major features that stand apart from the contextual-command core live
as optional modules under =modules/=, toggled independently of
=maf-mode=. A module is nothing but a global minor mode: it installs
everything it needs when enabled and removes it all when disabled, and
it works on its own, =M-x=-toggled like any minor mode, whether or not
the module system is loaded. "Module" is just our name for one of
these modes.

The registry in =core/maf-module.el= adds list-driven management on
top. Loading a module file registers its mode under a short name with
=maf-register-module=; =maf-modules= (a defcustom) lists the names that
should be active. The list and the live modes stay in sync in both
directions:

- On activation, =maf-modules-apply= drives the modes from the list,
  enabling the listed modules and disabling the rest — run at load and
  whenever =maf-modules= is set through Customize.
- When a mode is toggled directly, its hook writes the list back from
  live state (=maf-module--reconcile=), so =maf-modules= stays an
  accurate record of what is on. A dynamic flag suppresses this while
  =maf-modules-apply= runs, so applying the list never turns around and
  rewrites it.

The registry hooks *into* the modes, never the reverse: a mode body
knows nothing of the registry, so each module guards its registration
(=(when (require 'maf-module nil t) ...)=) and the feature stays fully
usable with the registry absent — you just lose the list-driven
management.

=M-x maf-list-modules= opens =*maf-modules*=, a dial buffer
(=pkg/dial=) of every registered module as an on/off row, split into
its own window. =TAB= or =SPC= flips the module on the current line
in place: it calls the mode directly, so the
reconcile above keeps =maf-modules= in step. Each module supplies its
own description as the third argument to =maf-register-module=,
echoed as point rests on its row, so the menu documents whatever is
loaded without a central list to maintain. A description is written
in two parts — one short sentence saying what the module does, a
blank line, then three or four lines on what that means in practice.
The menu heads the echoed text with the module's name, since help
left standing in the echo area outlives the move off its row;
Customize, which has room for one line, takes the summary alone.

Two module shapes have appeared, matching the feature's state:

- *Global mode.* The history module (=modules/maf-history.el=) is one
  =maf-use-history-mode= global minor mode: enabling installs the recording
  hook and the =calc-record= advice and binds =t d= to =maf-history= in
  =maf-mode-map=; disabling removes them and cedes =t d= back to calc's
  =calc-trail-display=. Browsing is two side-by-side
  windows below calc: =*maf-history*= (=maf-history-mode=) on the left
  is the action log, one line per state, newest at the top, each
  prefixed by a git-style change marker (=+= added, =-= removed, =~=
  changed in place, =·= for the oldest state, which has nothing to
  diff against), the current one marked;
  =*maf-history-stack*= (=maf-history-stack-mode=,
  whose map inherits the log's) on the right shows that state's whole
  stack. Moving in the log re-renders the stack beside it, so the
  session's course reads at a glance while stepping.
  Persistence
  (=maf-persist-mode=) is the same shape: enabling installs the save
  timer and the restore hook and binds =t l= to
  =maf-restore-stack-from= (a key calc leaves free, beside the
  history's =t d= — both bring back an earlier stack, the history's
  from this session and =t l='s from any session) and =t u= to
  =maf-save-stack=, its on-demand companion; disabling removes them.
  The edit module too
  (=modules/maf-edit.el=): =maf-use-edit-mode= installs SPC / ` / C-o /
  =(= in =maf-mode-map= when enabled and cedes them
  back when disabled — its body is just those bindings, the on-demand
  editing session (=maf-edit-mode=, turned on per gesture, not a
  feature switch) being separate.

- *Globalized buffer-local mode.* Highlighting is inherently
  per-buffer (an overlay per calc buffer), so =modules/maf-hl.el= keeps
  the buffer-local =maf-hl-mode= and wraps it in a
  =define-globalized-minor-mode=, =maf-use-hl-mode=, which turns the
  buffer-local mode on in every calc buffer. That globalized mode is
  the registered toggle; =M-x maf-hl-mode= still works standalone in a
  single buffer. The preview module (=modules/maf-preview.el=) is the
  same shape: =maf-use-preview-mode= turns the buffer-local
  =maf-preview-mode= on in every calc buffer, showing the entry at point
  — as it stands on the stack, unsimplified — in Big display in a
  floating panel at the top-right of the calc window. On a graphical frame the panel is a =posframe= child frame:
  parented to the calc frame and focus-refused, it stays out of the
  window manager's reach (no Alt-Tab, no focus). Where a child frame
  cannot be had — a text terminal, or no =posframe= — the same panel is
  drawn inside the window itself, one overlay per line it covers, each
  carrying a row of the panel and replacing the tail of a stack line
  long enough to reach it, so the panel occludes the stack rather than
  displacing it. Either way the panel is anchored to the top of the
  window, not to the text, so it stays in view regardless of how far the
  stack is scrolled — unlike a plain in-buffer overlay, which scrolls
  away once the stack outgrows the window. It hides itself while the
  whole buffer is already in Big display and during a =maf-edit=
  session.

A module owns its whole lifecycle, including bindings: =maf-mode= no
longer enables highlighting or the history, and =bindings.el= no longer
holds the =t d= or in-place-editing keys.

The default =maf-modules= holds the features that belong in the base
UX — =maf-history=, =maf-hl=, =maf-edit=, =maf-preview=, and =maf-formulas=.
The persist module
(=modules/maf-persist.el=) is the exception: it writes save files to disk,
so it stays out of the default and is opt-in — the dev instance adds
=maf-persist= to =maf-modules= in =project-init.el= and calls
=maf-modules-apply=, the documented Lisp way to enable a module.

* See also

- [[file:../../core/maf-resolve.el][core/maf-resolve.el]] — context schema reference at top of file
- [[file:calc-ref.org][calc-ref.org]] — calc API reference
