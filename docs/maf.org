#+title: maf

* docs
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Lisp-Definitions][lisp definitions]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Stack-Access][stack access]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Symbolic-Lisp-Functions][symbolic lisp functions]]

* Todo
  - [-] preview as latex
  - [ ] *maf-formulas*: add basic/fundamental identities (addition,
    multiplication, etc)

* review
  - [ ] *maf-persist*
    - [ ] `l R` should bring up a buffer (using dial pkg) instead of a menu
      - the buffer should have all the entries
      - `D` to delete entry
      - preview each entry in a split window when point hovers over
      - standard npjk navigation for prev/next entry
  - [-] add redundant binding `l t` for apart (split fraction) command (`a a`)
    this acts as an analog to `l c` (collect)
  - [ ] change *maf-history* rendering
    - it should render as two columns
    - the left column showing the actions
    - the right column showing the corresponding stack
  - [ ] new command: jump to next operand
    - bind to `S-SPC`
    - do not bind in edit/editplus, leave unbound
    - delete previous command bound to `S-SPC` (jump to next spc char)
  - [ ] rebind `,` to simplify (currently `k k`)
    - [ ] rebind former `,` (push variable) to `:`
    - `k k` should be free now. can we do anything with this?
  - [ ] swap `C-RET` and `M-RET`
  - [ ] new `C-RET` should retain point, otherwise there's no difference against
    `RET`
  - [ ] (a + b) (2 c - d) latex is wrong (remove the \times)
  - [ ] how to sort vector? did this used to be `v o`?
  - [ ] rational roots theorem
    what command used to take something like:
    2: [1, 3, 9]
    1:  [1, 2, 3, 6]
    and produce all unique

* major features [8/9]
  - [X] bindings module
  - [X] Persistent sub-formula highlighting
  - [X] History module (né timeline)
  - [X] Omni-options menu buffer
  - [X] Formulas module
  - [X] Modules manager
  - [X] Use module architecture (maf-history, maf-edit)
  - [X] Recall ring for typed entries (maf-recall)
  - [ ] Discovery document for useful features, behaviors, modules, etc

* feedback
  - [ ] single-key (no modifiers) to force next cmd to target line
  - following cmds should be bound to single keys (no modifiers):
    - [X] home (make sure to set mark)
    - [ ] expand
    - [ ] simplify
    - [ ] factor
    - [ ] target-line

* later::plotting module
  - [ ] my/calc-graph-quick / -with-range (g l / g i) — plot the top entry straight to
    gnuplot (see Todo: interval form for the range prompt)
  - [ ] my/calc-to-gnuplot-string — calc expr → gnuplot formula (fortran mode + fixups)
  - [ ] gnuplot session defaults (index.el) — resolution 1000, qt device, seeded
    \"set grid / set nokey\"
  - [ ] g i (my/calc-graph-quick-with-range) interval form a:b should work with spaces 'a b'

* later
** thin web client w/ mathjax rendering
** rework preview
   it feels awkward in actual usage
** modules suboptions

* maybe
** TODO preview entry as latex

* cleanup
  - [ ] delete my/calc
  - [ ] use stock bindings
  - [ ] generate tests full coverage
  - [ ] remove seed entries
  - [ ] remove project-init.el
