#+title: maf

* Docs
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Lisp-Definitions][lisp definitions]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Stack-Access][stack access]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Symbolic-Lisp-Functions][symbolic lisp functions]]

* Plan (macro)
  1) [ ] Move all of relevant my/calc config into project
  2) [ ] Harden through using w/ anki math

* Major features [4/9]
  - [ ] Vim bindings module (move by noun, verb, char)
  - [X] Persistent sub-formula highlighting
  - [X] Timeline module
  - [ ] Graphing module
  - [ ] Omni-options menu buffer
  - [X] Formulas module. Support categories, docs, examples
    - [X] Set default binding
  - [X] Modules manager: Buffer where you can toggle modules
    - [X] Create manager
    - [X] Set default binding
  - [X] Use module architecture (maf-timeline, maf-edit)
  - [ ] Discovery document for useful features, behaviors, modules, etc

* Feedback
  - [X] command to isolate current subexpr (and delete rest of entry)
  - [-] single-key (no modifiers) to force next cmd to target line
  - following cmds should be bound to single keys (no modifiers):
    - [ ] home (make sure to set mark)
    - [ ] expand
    - [ ] simplify
    - [ ] factor
    - [ ] target-line
  - maf-editplus module
    - [-] tab to break out of parens
    - [ ] make s-tab work also to move backward
  - formulas
    - [X] q to quit filtered results mode
    - [X] bind tab to move between groups
    - [X] add recents group (should be the first group)
      - shows recently used formulas from current session (don't persist across
        sessions)
    - [X] filter in real-time as search string changes

* Low-hanging fruit
** TODO Port the edit-mode insertions (modules/maf-edit.el)
   - [ ] P → pi
   - [ ] M-2..M-9
   - [ ] backslash → sqrt
   - [ ] M-' → ^n and ^()
   - [ ] \ L | wrap helpers around the preceding expression or region.
   - [X] my/calc-edit-wrap-parens

* Todo
# NOW
** IN-PROGRESS dwim map commands
   avoid having separate map commands for vectors and equations
   bind `M` to map from algebraic entry input (normally `a M '`)
   bind `$` to map from stack, m=1 (normally `a M $`)
** IN-PROGRESS stack/edit add command to move to parent expression.
   1:  y = 2 (x| + 3)^2 - 12
   ->  y = 2 (x + 3)|^2 - 12
** TODO distribute and merge should operate contextually.
** TODO move all options to unified configuration view.
   - Free up option prefix bindings (eg, m).
** IN-PROGRESS quick recall command, bind to M-[n/p] to recall previous/next bare
   (starting from empty) edit-mode entries. The purpose of this is to avoid the
   pattern of having to duplicate or store an entry that you might want to reuse
   or recall in case you screw up a calculation.

* Port
  - [-] Port my/calc/* features one-by-one.
  Full inventory of ~/.emacs.d/my/calc below; legacy bindings in parens
  are reference only, maf rebinds at port time. Ported or superseded
  features are summarized at the end of each section.

* PORT:stack.el
** Motion
   - [ ] my/calc-forward-noun / my/calc-backward-noun (M-f/M-b) — next/previous number or
     variable, skipping function names; fix the line-prefix bug while at it (see Bugs)
** Variables
   - [ ] my/calc-recall-browse (p) — completing-read recall, values as annotations
     (drop the ivy-specific TAB cycling)
** Polynomials
   - [ ] Polynomial term ordering — math-normalize advice keeping sums in descending degree
** Triangles
   - [ ] calcFunc-hypot advice — symbolic args: sqrt(abssqr(a) + abssqr(b));
     still open, f h alone. maf--cath needs none of it: squaring under a
     structural math-normalize already reduces sqrt(2)^2 to 2.

* PORT:selection.el
  - [ ] my/calc-sel-distribute (j D) — contextual, no-simplify, frac→division fix (see BACKLOG)
  - [ ] my/calc-sel-merge (j M) — contextual, unselect after

* PORT:edit.el
  maf-edit replaces the calc-edit buffer workflow itself; these are the
  in-entry conveniences worth recreating in maf-edit / digit entry:
  - [X] Make this a module — modules/maf-editplus.el, the home for the
    in-session keys; installs them into maf-edit-mode-map
  - [ ] Entry history (M-p / M-n) — recall previous entries (see Todo)
  - [X] my/calc-vector-edit \"(\" — start a bracketed vector entry —
    ported as maf-edit-add-vector
  - [ ] Wrap helpers (\\ L |) — sqrt/ln/abs around the preceding expression or region
  - [ ] my/calc-edit-square-dwim / -cube-dwim (: W \") — insert ^2/^3; repeats increment
  - [ ] Power shortcuts (M-2..M-9, M-') — insert ^n / ^()
  - [X] my/calc-edit-escape-parens (TAB) — ported as
    maf-editplus-escape-group; scans maf-edit's own way (any closer
    matches any opener, bounded to the entry) instead of syntax-ppss
  - [ ] my/calc-edit-wrap-parens (M-o) / -toggle-brackets (S-up/down)
  - [ ] my/calc-edit-jump-sides — jump between the sides of =, inserting \" = \" if absent
  - [ ] my/calc-edit-insert-pi (P); J → *
  - [ ] my/calc-edit-duplicate (M-RET) — duplicate line, comma-aware (vector entry)
  - [ ] my/calc-duplicate-paren-expr (C-RET) — duplicate innermost paren group
  Already covered: edit-dwim/edit-selection → maf-edit (SPC); no-simplify
  commit is maf-edit's default commit path; ; → : is
  maf-edit-insert-colon, with the literal ; moved to M-;.

* PORT:plot.el
  - [ ] my/calc-graph-quick / -with-range (g l / g i) — plot the top entry straight to
    gnuplot (see Todo: interval form for the range prompt)
  - [ ] my/calc-to-gnuplot-string — calc expr → gnuplot formula (fortran mode + fixups)
  - [ ] gnuplot session defaults (index.el) — resolution 1000, qt device, seeded
    \"set grid / set nokey\"
  - [ ] g i (my/calc-graph-quick-with-range) interval form a:b should work with spaces 'a b'

* Bugs
** BUG (my/calc-backward-noun) and (my/calc-forward-noun) should ignore line prefix.

* BACKLOG
** Issue: j D (my/calc-sel-distribute)
   Doesn't work contextually. Should work like 'j M'. Work on stack item when at home.
** Instead of having to use the default bindings 'a m &', can you make 'a m i' work?
   This should invert both sides of an equation. Normally the binding would use
   '&' instead of 'i', but 'i' is more convenient and I have that bound to
   'calc-inv' The issue is that the 'a m' sequence expects standard keybindings.

* Package
  Config file with all config settings and default values
  Alternative graphing
  Alternative trail. Should support time travel and finding old single entries.
  Use transient mode for prefix commands
  Swap _ and -

* Saved for later
  - Rebind formula manager
  - Rebind module manager
  - Figure out bindings
    - Record which bindings/commands are most used

* Cleanup
  - [ ] Delete my/calc
  - [ ] Use stock bindings
  - [ ] Generate tests to cover maf. Step tests when testing end-to-end. Unit
    tests for basic stuff.
  - [ ] Remove seed entries
  - [ ] Remove project-init.el
