#+title: maf

* docs
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Lisp-Definitions][lisp definitions]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Stack-Access][stack access]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Symbolic-Lisp-Functions][symbolic lisp functions]]

* NOW
  - *maf-modules*
    - [X] rewrite detailed help text in simpler terms and use simple clarifying
      examples when possible or useful
  - [ ] find better binding for simplify (`a s`)
  - [ ] when maf-preview is toggled, it should show the preview for the current
    entry immediately
  - [ ] `a l` should take a variable
  - [ ] `M-r` to get all roots

* review
  x^2 - 2 x - 8 = 27
  `j e` doesn't work properly here at the 27

  () entry in edit mode should resolve to nothing

  keep trail bindings (`t d`, etc)
  *maf-history* doesn't replace trail, it offers an alternative, but the
  existing trail should still be available

  edit mode, rebind `:` -> `'`

  *maf-history* bind C-M-k to delete all history
  title should be maf-history
  and the counter x/y should go above the row of actions
  `G` should also go to end (keep `>` binding also)
  start at the end on maf-history command`

  remap `M :` to `M M`

  `M <command>` doesn't process digit input correctly

  remap simplify `a s` -> `k k`
  (`c` is available also...)
  remap previous `k k` -> `k v`

  unbind `j x` and `j f`. we'll use the default values `j D` and `j M`

  simplify should simplify these obvious types of expressions. why doesn't it?
  3^log((x + 2) (x - 4), 3) = 27

  editplus: 24x|
  wrapping commands (sin, cos, tan, squaring, sqrt, etc) here actually should target just the `x`
  we reverted this at some point, but i was wrong
  `x` is the smallest expression preceeding point, so it's consistent with what we actually want and expect

* major features [8/9]
  - [X] bindings module
  - [X] Persistent sub-formula highlighting
  - [X] History module (né timeline)
  - [X] Omni-options menu buffer
  - [X] Formulas module
  - [X] Modules manager
  - [X] Use module architecture (maf-history, maf-edit)
  - [X] Recall ring for typed entries (maf-recall)
  - [ ] Discovery document for useful features, behaviors, modules, etc

* feedback
  - [ ] single-key (no modifiers) to force next cmd to target line
  - following cmds should be bound to single keys (no modifiers):
    - [X] home (make sure to set mark)
    - [ ] expand
    - [ ] simplify
    - [ ] factor
    - [ ] target-line

* later::plotting module
  - [ ] my/calc-graph-quick / -with-range (g l / g i) — plot the top entry straight to
    gnuplot (see Todo: interval form for the range prompt)
  - [ ] my/calc-to-gnuplot-string — calc expr → gnuplot formula (fortran mode + fixups)
  - [ ] gnuplot session defaults (index.el) — resolution 1000, qt device, seeded
    \"set grid / set nokey\"
  - [ ] g i (my/calc-graph-quick-with-range) interval form a:b should work with spaces 'a b'

* later
** rework preview
   it feels awkward in actual usage
** modules suboptions

* maybe
** TODO preview entry as latex

* cleanup
  - [ ] delete my/calc
  - [ ] use stock bindings
  - [ ] generate tests full coverage
  - [ ] remove seed entries
  - [ ] remove project-init.el
