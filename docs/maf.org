#+title: maf

* Docs
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Lisp-Definitions][lisp definitions]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Stack-Access][stack access]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Symbolic-Lisp-Functions][symbolic lisp functions]]

* Plan (macro)
  1) [ ] Move all of relevant my/calc config into project
  2) [ ] Harden through using w/ anki math

* Major features [7/10]
  - [ ] Vim bindings module (move by noun, verb, char)
  - [X] Persistent sub-formula highlighting
  - [X] Timeline module
  - [ ] Graphing module
  - [X] Omni-options menu buffer
  - [X] Formulas module
  - [X] Modules manager
  - [X] Use module architecture (maf-timeline, maf-edit)
  - [X] Recall ring for typed entries (maf-recall)
  - [ ] Discovery document for useful features, behaviors, modules, etc

* Feedback
  - [ ] single-key (no modifiers) to force next cmd to target line
  - following cmds should be bound to single keys (no modifiers):
    - [X] home (make sure to set mark)
    - [ ] expand
    - [ ] simplify
    - [ ] factor
    - [ ] target-line

* map
let's work out the map feature
we've worked on this before but i want to make it's correct
this needs a few parts:
1) a binary command (bound to `$`) to map top of stack (the mapping function)
   against the target entry (the data structure, either an equation or vector)
   - if vector, apply the mapping function at m=1 over all the vector elements
   - if equation, apply the mapping function to both sides of the equation
   - this command combines the builtin calc functions to EITHER map over an
     equation of vector. there's no good reason to have separate commands for
     this when we could instead have just a single mapping command.
   - this seems to match the current `$`
2) similary to (1), but is unary. the mapping function, instead of being taken
   from the stack m=1, take it from minibuffer input. bind to `M`. this is a
   builtin command that alreadyd exists, but does not work contextually, and
   also uses a separate command for equations and vectors.
3) a command which sets a new FLAG (like keep-args `K`, or hyper `H`). when this
   flag is set, the next command maps over the entry or vector. the command
   could be either binary or unary - it should work either way. bind to `W` for
   now.
   - this is built into calc already, but works differently. it doesn't set a
     flag, but instead uses a prefix and the user supplies the next command
     given a keymap. our method is much more flexible and future-facing because
     it doesn't require a hardcoded keymap which supports a limited set of
     commands, but instead works with any command which processes a MAP FLAG.

if necessary or useful, just undo the existing maf-map functionality and start
over from scratch.

* Todo
# NOW
** TODO move all options to unified configuration view
   - Free up option prefix bindings (eg, m).
** TODO preview entry as latex
** IN-PROGRESS issue with map, mapping actual commands
   eg, can't map command against vector

* Port
  - [-] Port my/calc/* features one-by-one.
  Full inventory of ~/.emacs.d/my/calc below; legacy bindings in parens
  are reference only, maf rebinds at port time. Ported or superseded
  features are summarized at the end of each section.

* PORT:plot.el
  - [ ] my/calc-graph-quick / -with-range (g l / g i) — plot the top entry straight to
    gnuplot (see Todo: interval form for the range prompt)
  - [ ] my/calc-to-gnuplot-string — calc expr → gnuplot formula (fortran mode + fixups)
  - [ ] gnuplot session defaults (index.el) — resolution 1000, qt device, seeded
    \"set grid / set nokey\"
  - [ ] g i (my/calc-graph-quick-with-range) interval form a:b should work with spaces 'a b'

* Cleanup
  - [ ] Delete my/calc
  - [ ] Use stock bindings
  - [ ] Generate tests full coverage
  - [ ] Remove seed entries
  - [ ] Remove project-init.el
