x#+title: maf

* docs
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Lisp-Definitions][lisp definitions]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Stack-Access][stack access]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Symbolic-Lisp-Functions][symbolic lisp functions]]

* tasks
do each of these tasks in order and create a commit after each one, but don't
push yet:

1) [ ] *maf-formulas* add standard formula for `segment addition postulate`
2) [ ] bug: entries like `tan(x + pi)` can't be plotted (i tested using `g l`
   command) because plotter treats `pi` as an independent variable
3) [ ] update the website by taking the math problem:

     Rewrite the equation of the vertical parabola in standard form:
     x^2 + 2x + 8y + 17 = 0

   and add a `showy` example to the website (add as last example). this will be
   intended to showcase different features of `maf`.

   in this order, demonstrate solving the problem:

   # step 1: first show the example
   x^2 + 2x + 8y + 17 = 0
   |.

   # step 2: `j c` to collect `x` terms on one side
   x^2 + 2 x = -8 y - 17
   |.

   # step 3: move point
   x^2 |+ 2 x = -8 y - 17
   .

   # step 4: `k s` complete the square over the expression
   (x + 1)^2 |- 1 = -8 y - 17

   # step 5: move point
   (x + 1)^2 - |1 = -8 y - 17

   # step 6: `j e` move term to other side
   (x + 1)^2 = |1 - (17 + 8 y)

   # step 7: move point
   (x + 1)^2 = 1 |- (17 + 8 y)

   # step 8: `k k` simplify expression on RHS
   (x + 1)^2 = -8 y |- 16

   # step 9: `l F` factor out the gcd
   (x + 1)^2 = -8| (y + 2)

   # step 10: `g l` show a plot

4) [ ] pilot emacs, going through the various features (especially the modules) and
   take screenshots AND export as html/css (emacs has a built-in command to do
   this, `htmlfontify-buffer`)

   these screenshots will be showcased on the website to show the various
   features of maf.

* review
** website
   - [ ] website: interactive maf stack emulator w/ step function
   - [ ] website: animate typing, character entry.
   - [ ] website: less stuff, less content, concentrate, focus
   - [ ] website: mvp; animated examples, additions, most important bindings
     (module bindings, editing)
** commands
** maf-history
** maf-editplus
** maf-formulas
** maf-pretty

* IN-PROGRESS website
  - [ ] generate readme
  - [ ] generate website
    - [ ] put inside public/ directory
    - [ ] stick to plain html/css/js
    - [ ] show all major features
    - [ ] show all additions
    - [ ] show all changes
    - [ ] show how to use / install
    - [ ] show deps
    - [ ] crawl through every single commit and build a data structure with
      { date, changes, additions } fields (anything else?)
    - [ ] bindings cheat sheet page. on hover, show the example and description.
      also show `H` and `I` flag behavior. group the bindings by category.
  - [-] check for accuracy with other agent

* website 2
  - [ ] create video/gif examples
  - [ ] try to get into melpa
  - [ ] post on reddit / forums
  - [ ] show images. click image to play video

* major features
  - [ ] discovery document for useful features, behaviors, modules, etc
  - [ ] readme (overview)
  - [ ] technical docs for devs

* when done
  - [ ] revise and update all anki cards in `calc-mode` deck
    - base on `ergo` profile
    - update outdated bindings
    - add cards for missing bindings

# much later below

* later :: standalone web app
  - multi-pane design
  - pane 1 (main): calc stack
    - entries show text and mathjax rendering
  - pane 2: current plot
  - pane 3: modules
  - pane 4: options
