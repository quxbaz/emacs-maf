#+title: maf

* docs
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Lisp-Definitions][lisp definitions]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Stack-Access][stack access]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Symbolic-Lisp-Functions][symbolic lisp functions]]

* Todo
  - [-] preview as latex
  - [ ] *maf-formulas*: add basic/fundamental identities (addition,
    multiplication, etc)

* now

* review
  - *maf-history* rendering
    - refine history log
      - [ ] if action is duplication (eg, RET on an entire entry), then action
        should be "dupe"
      - [ ] curreently on "add" items, the added item is not highlighted in
        yellow in the corresponding stack
  - *maf-persist*
    - [-] bind new command to name session `R`
  - *maf-formulas*
    - [-] in details view, the variables in the "big" formula (referenced below)
      should match the color of the referenced value (looks like teal currently)
  - [-] S-SPC doesn't work propery. it should only land on operands, but it
    lands on terms also.
  - [ ] bug:
    given,
    1: log(x|, 5)
    any wrapping function (eg, pow/sqrt/sin/etc) results in:
    => log(x, 5)^2
    instead of,
    => log(x^2, 5)
  - [ ] bind `j U` to the contextual version of `M-u`
  - [ ] bind `C-M-SPC` to the reverse of `S-SPC`. move to the PREV operand
    - same conditions. only work in stack mode.
  - [ ] find command that solves with +/i in answer, but uses a different
    representation (port it)
  - [ ] ` command should edit stack entry at m=1
  - [ ] bug: `v o` sort command is completely broken given this entry,
    [sqrt(10), -sqrt(10)]
  - [ ] bug: given,
    (x - 2|)^2 = -12 y
    `G`, `k k`, `G`
    point returns the the former line, but not to the same column (it lands in
    prefix area instead)

* major features [8/9]
  - [X] bindings module
  - [X] Persistent sub-formula highlighting
  - [X] History module (né timeline)
  - [X] Omni-options menu buffer
  - [X] Formulas module
  - [X] Modules manager
  - [X] Use module architecture (maf-history, maf-edit)
  - [X] Recall ring for typed entries (maf-recall)
  - [ ] Discovery document for useful features, behaviors, modules, etc

* feedback
  - [ ] single-key (no modifiers) to force next cmd to target line
  - following cmds should be bound to single keys (no modifiers):
    - [X] home (make sure to set mark)
    - [ ] expand
    - [ ] simplify
    - [ ] factor
    - [ ] target-line

* later::plotting module
  - [ ] my/calc-graph-quick / -with-range (g l / g i) — plot the top entry straight to
    gnuplot (see Todo: interval form for the range prompt)
  - [ ] my/calc-to-gnuplot-string — calc expr → gnuplot formula (fortran mode + fixups)
  - [ ] gnuplot session defaults (index.el) — resolution 1000, qt device, seeded
    \"set grid / set nokey\"
  - [ ] g i (my/calc-graph-quick-with-range) interval form a:b should work with spaces 'a b'

* later
** thin web client w/ mathjax rendering
** rework preview
   it feels awkward in actual usage
** modules suboptions

* when done
  - [ ] revise and update all anki cards in `calc-mode` deck
    - base on `native` profile
    - update outdated bindings
    - add cards for missing bindings

* maybe
** TODO preview entry as latex

* cleanup
  - [ ] delete my/calc
  - [ ] use stock bindings
  - [ ] generate tests full coverage
  - [ ] remove seed entries
  - [ ] remove project-init.el
