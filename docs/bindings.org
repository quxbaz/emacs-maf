#+title: bindings module plan

* concepts
  - the `maf-bindings` module controls whether maf bindings are active at all
  - a *profile* selects the key layout: `calc`, `native` (default), `vim`
  - the bindings are what's native, not the commands: every profile runs
    mafcmds — the calc profile means calc's *layout*, maf's *commands*

* architecture: hub and spokes
  - one shared *base map*; each profile map is an independent sibling
    inheriting from base — no profile references another profile's
    *map*. The one sanctioned relationship is *derivation*, resolved
    at compile time: a profile registered with :derive compiles the
    source's effective defaults beneath its own declarations, an own
    claim displacing whatever it overlaps (vim derives from native
    this way). The compiled maps stay independent siblings
  - base holds *command remaps only*, so it claims no keys and cannot
    collide with any profile's layout:
    #+begin_src elisp
      (define-key maf-bindings-base-map [remap calc-plus] #'mafcmd-add)
    #+end_src
  - remaps are audited (allowlist), not automatic: maf deliberately keeps
    some calc originals reachable as escape hatches (j E vs j e), and a
    remap would silently convert them. Excluding from the allowlist *is*
    the escape hatch — no wrapper commands
  - beneath base sits the *escape map*, base's parent: the keys that
    exist in every profile and even with the module off. It holds one —
    `m c`, the module menu (`maf-list-modules`) — the deliberate
    exception to the calc profile's untouched-layout promise and to
    off's zero keys, because that buffer is where profiles and modules
    are turned back on; anywhere less durable and the way back is M-x
    by name
  - profile deltas are declared, and a declaration may target several
    profiles (source-level sharing); a profile that is another plus a
    delta says so once with :derive instead of every declaration
    naming it:
    #+begin_src elisp
      (maf-bindings-define '(calc native) "x" #'mafcmd-expand)
      (maf-bindings-defprofile 'vim :derive 'native)
      (maf-bindings-define '(vim) "w" #'maf-forward-noun)
    #+end_src
  - `maf-mode-map` stays the one registered map (stable dispatcher);
    switching = repointing its parent, live, nothing rebuilt:
    #+begin_src elisp
      (maf-bindings-set-profile 'calc)
    #+end_src

* lookup order (per profile)
  1. user bindings (per-profile user map first, then the common user map)
  2. enabled-module declarations
  3. profile defaults (minus compiled suppressions)
  4. base remaps
  5. the escape map (`m c`, the module menu) — also the *whole* map
     while the module is off
  6. calc's own map

* user configuration
  - user maps are never written or rebuilt by maf; they survive
    switches. Named maf-<profile>-user-map — `maf-calc-user-map`,
    `maf-native-user-map`, `maf-vim-user-map`, `maf-common-user-map` —
    and defprofile generates the name for a custom profile
    (maf-my-dvorak-user-map). Internal generated maps keep the long
    `maf-bindings-` names
    #+begin_src elisp
      (setq maf-bindings-profile 'vim)   ; before maf loads;
      ;; after load: M-x maf-bindings-set-profile, or customize
      (define-key maf-vim-user-map (kbd "g") #'maf-go-home)
      (define-key maf-common-user-map (kbd "<f6>") #'maf-timeline)
    #+end_src
  - principle: the user surface is plain elisp — real keymaps,
    `define-key`, `kbd` — for everything a keymap can express: adding,
    moving, overriding
  - the one exception is removal, which no keymap entry can say (nil
    exposes what lies below, `undefined` blocks calc too, and a sentinel
    command in the user map would shadow the fall-through it promises).
    So removal is the one API call:
    #+begin_src elisp
      (maf-bindings-unbind 'native ">")  ; > falls through to calc
    #+end_src
    suppressions live in the registry; the compiler omits the default
    from the generated map, so lookup falls through naturally, C-h k
    names the effective lower command (calc's own, or the maf sibling
    where a base remap applies), and the call itself triggers the
    recompile. `maf-bindings-restore` is the inverse — it drops the
    suppression and the profile default returns. Scope: suppression removes *generated key declarations*
    — profile defaults and module declarations alike, for that profile;
    a suppressed prefix key (j) removes every generated descendant and
    exposes calc's whole prefix. Base remaps are not key declarations
    and still apply: suppression exposes calc's *layout*, but a calc
    command with a maf sibling still runs the sibling — suppressing +
    can never produce raw calc-plus
  - users can declare their own profiles — a profile is just a
    registered name plus two maps over base, nothing about the built-in
    three is special:
    #+begin_src elisp
      (maf-bindings-defprofile 'my-dvorak :clone 'native)
      (maf-bindings-define '(my-dvorak) "n" #'mafcmd-solve)
      (maf-bindings-set-profile 'my-dvorak)
    #+end_src
    tweaks to an existing profile don't need this — that's the user
    map's job; a profile earns its existence as a whole switchable
    layout
  - clone copies the source profile's *own defaults only*, at the
    declaration level; module keys are never copied — they follow the
    module's own targeting, live
  - ships after the built-ins work: custom profiles need the
    profile-aware displays, so both land together

* migrating an init
  - a direct `(define-key maf-mode-map ...)` in an init still works —
    the map's own entries outrank the parent composition — but it is
    outside the system: it survives profile switches indiscriminately
    and suppression cannot see it. Move it to the profile's user map
    (or `maf-common-user-map`):
    #+begin_src elisp
      ;; before                                  after
      (define-key maf-mode-map (kbd "g") #'maf-go-home)
      (define-key maf-native-user-map (kbd "g") #'maf-go-home)
    #+end_src

* rules
  - dogfooding: the built-in profiles are declared through the same
    public API a user would use — `maf-bindings-defprofile` +
    `maf-bindings-define`, no privileged internal path. The built-ins
    are ordinary registry entries that happen to ship with maf, and
    double as the reference example for writing your own
  - modules declare their keys to the binding system; no raw define-key
    into `maf-mode-map` (a toggle must never erase a user override).
    Module targets are explicit profile names, `:all` deliberately —
    strictness governs modules too: under strict, the calc profile gets
    no direct module key declarations, though a module command that is
    a genuine calc sibling may still arrive as a remap (audited like
    base remaps — an escape-hatch original is a reason not to); under
    curated additions, also keys that displace nothing (options' ?
    shadows calc-help, so not there either way). Follows the strictness
    ruling below
  - registrations are owned and replaced as whole sets, so a reload is
    safe by construction: defprofile replaces that profile's default
    set, a module registration replaces that module's set, generated
    maps rebuild from the registry — a declaration deleted from source
    is gone after reload. User maps are never rebuilt
  - profiles govern the stack map and the digit-map overrides only;
    maf-edit, timeline, formulas, options keep their own fixed maps
  - digit-map changes are tracked installs: each declaration names the
    stock calc binding it expects to replace; install only when the
    current binding equals it (a user's direct customization is left
    untouched), save it, and uninstall restores it only if the current
    binding is still the one maf installed
  - key displays (module help, dial legends) derive keys from the active
    profile — no static key strings
  - the *maf-modules* row for maf-bindings is a profile picker, not a
    toggle: `off | calc | native | vim | ...` with the user's custom
    profiles listed too (they appear when the menu opens). off is the
    module off — stock calc, no maf keys but the escape map's `m c`,
    the way back to this menu
  - the registry compiler validates at build time: a key cannot be both
    a command and a live prefix in one profile; module/profile and
    module/module key conflicts are errors, not precedence accidents

* calc profile
  - promise: zero surprises for a calc user; layout untouched. The one
    exception is the escape map's `m c` (unbound in calc itself) —
    every profile carries it, this one included
  - siblings run via base remaps on calc's own keys (+, a x, a S, ...)
  - keys whose calc command has no sibling stay calc's (x, h, scrolling)
  - no navigation bindings: silence means stock calc/emacs lookup

* profile:native
  What today's layout is, beyond calc's own keys. The shadowing and
  additions groups below are declared `'(native)` — src/bindings.el is
  the native layout's file. calc and vim carry only the sibling groups
  (and, pending the module strictness ruling, the module keys); vim
  stays identical to calc until its content phase lays it out.

  Policy (2026-08-19): `k` and `l` are native's custom-command
  prefixes. A maf command may take a weak key there without a thematic
  case — `k s` complete-square and `k d` factor-powers are such claims
  — and new custom bindings reach for these two prefixes first. The
  sitting residents (combinatorics siblings under k, log-units under
  l) keep their stock keys until a custom command actually claims one:
  eviction is lazy, per key, never a sweep. The bulk first:
  - the mafcmd table: ~200 contextual siblings shadowing their calc
    originals on calc's own keys (the key column in src/maf-cmds.el)
  - base remaps: undo, undo-redo, calc-undo, calc-redo → maf-undo/redo
  - the escape map beneath base: `m c` maf-list-modules — in every
    profile and with the module off, not a native addition
  - module keys: `SPC` \` `C-o` `(` (edit), `?` (options), `t d` `M-h`
    (timeline), `t l` `t u` (persist), `M-p` `M-n` (recall), `s o`
    (formulas)
** contextual siblings on their own calc key (outside the table)
   - `A` mafcmd-abs ← calc-abs, `f h` mafcmd-hypot ← calc-hypot,
     `a b` mafcmd-substitute ← calc-substitute,
     `a .` mafcmd-remove-equal ← calc-remove-equal,
     `v u` mafcmd-unpack ← calc-unpack (also `j U`, `j M-U` ←
     calc-sel-unpack), `P` maf-pi ← calc-pi
   - `U` maf-undo ← calc-undo, `D` maf-redo ← calc-redo,
     `C-d` / `DEL` maf-del ← calc-pop, `C-k` maf-kill ← calc-kill,
     `M-w` maf-copy ← calc-copy-region-as-kill,
     `C-y` maf-yank ← calc-yank,
     `RET` maf-dup-or-clear-selections ← calc-enter,
     `TAB` / `C-t` maf-swap-up ← calc-roll-down,
     `M-RET` maf-dup-go ← calc-last-args
   - `r 0`..`r 9` maf-recall-quick ← calc-recall-quick
   - the digit starters `_` `0`..`9` `.` `#` maf-digit-start ←
     calcDigit-start
** shadowing (key taken from an unrelated calc command)
   - [X] `N` mafcmd-negate ← calc-eval-num
   - [X] `o` mafcmd-inv ← calc-realign
   - [X] `\` mafcmd-sqrt ← calc-idiv
   - [X] `<` mafcmd-decrement ← calc-scroll-left
   - [X] `>` mafcmd-increment ← calc-scroll-right
   - [X] `x` mafcmd-expand ← calc-execute-extended-command
   - [X] `X` mafcmd-log-exp ← calc-call-last-kbd-macro
   - [X] `k s` mafcmd-complete-square ← calc-stirling-number (a custom
     prefix claim — see the k/l policy above)
   - [X] `k d` mafcmd-factor-powers ← calc-double-factorial (a custom
     prefix claim; my/calc's key for the command too)
   - [X] `l f` mafcmd-factor-by ← calc-freq
   - [X] `i` mafcmd-solve-for ← calc-info
   - [X] `e` mafcmd-equal-to ← calcDigit-start
   - [X] `O` mafcmd-commute ← calc-option
   - [X] `j l` maf-commute-left ← calc-select-less
   - [X] `j r` maf-commute-right ← calc-rewrite-selection
   - [X] `j D` maf-distribute ← calc-sel-distribute (prompt-free version
     of the same operation)
     - resolved (2026-08-19): not folded into `x` — expand-all and
       expand-by-a-step are a useful distinction (`x` full depth over
       the target, `j D` one step at the site). A fold is mechanically
       sound (expand no-op => DistribRules), kept for later. `j x`
       added as a second key on expand's letter
   - [X] `j M` maf-merge ← calc-sel-merge (prompt-free version of the
     same operation)
     - `j f` added (2026-08-20) as a second key on factor's letter,
       mirroring `j x` for the distribute
   - [X] `v L` mafcmd-flatten ← calc-mlud
   - [X] `M` mafcmd-map-flag ← calc-more-recursion-depth
   - [X] `,` maf-quick-variable ← calc-comma
   - [X] `p` maf-browse-variables ← calc-precision
   - [X] `l d` mafcmd-to-degrees ← calc-db
   - [X] `f L` mafcmd-unit-cath ← calc-lnp1
   - [X] `M-k` mafcmd-coordinate-toggle ← calc-copy-as-kill
   - [X] `G` maf-go-home ← calc-argument
   - [X] `j e` maf-jump-equals ← calc-enable-selections
   - [X] `@` maf-toggle-simplify ← calcDigit-start
   - [X] `&` maf-toggle-big-language ← calc-inv
** additions (keys calc leaves unbound)
   - [X] `W` mafcmd-sqr
   - [X] `k k` mafcmd-evaluate
   - [X] `C-<return>` mafcmd-let
   - [X] `l l` mafcmd-float-frac (2026-08-20: the float/frac toggle —
     floats toward fractions first, fractions to floats otherwise,
     direction echoed; I forces float, H float-all, frac's tolerance
     on the prefix arg. mafcmd-float and mafcmd-frac stay defined,
     reachable by name and the flags; `l c` passed to
     collect-fractions)
   - [X] `l F` factor-gcd
   - [X] `l c` collect-fractions (2026-08-20: moved off `l t` onto the
     key the float/frac toggle freed — c for collect)
   - [X] `l v` inverse-function
   - [X] `a L` mafcmd-poly-lcm
   - [X] `M-i` mafcmd-auto-solve
   - [X] `l t` mafcmd-poly-roots (2026-08-20: moved off `M-r` onto the
     key collect-fractions vacated; M-r falls through to the global
     map)
   - [X] `a l` mafcmd-roots (alias of its stock `a P`; my/calc's key)
   - [X] `j i` mafcmd-isolate
   - [X] `a k` mafcmd-abs-ineq
   - [X] `M-.` mafcmd-remove-equal
   - [X] `l x` swap-vars
   - [X] `j j` mafcmd-raise
   - [X] `j x` maf-distribute (second key beside `j D`, on expand's
     letter: `x` expands all, `j x` one step at point)
   - [X] `j f` maf-merge (second key beside `j M`, on factor's letter:
     the inverse of `j x` on the letters of calc's a x / a f pair)
   - [X] `C-c C-c` mafcmd-esimplify (2026-08-20: second key beside its
     table key `a s`, earned by frequency; the mode's slice of the C-c
     convention, with its do-the-obvious-thing reflex)
   - [X] `S-<up>` / `S-<down>` mafcmd-toggle-op
   - [X] `C-|` mafcmd-bracket
   - [X] `v RET` maf-index
   - [X] `l g` unique-groups
   - [X] `r r` maf-recall-variable
   - [X] `M-o` mafcmd-mod-360
   - [X] `M-l` mafcmd-ref-angle
   - [X] `M-s` mafcmd-supplement
   - [X] `M-c` mafcmd-complement
   - [X] `l r` to-radians
   - [X] `f l` mafcmd-cath
   - [X] `<tab>` / `C-t` maf-swap-up
   - [X] `S-<return>` maf-roll-to-top
   - [X] `C-M-<backspace>` (and aliases) maf-roll-to-bottom
   - [X] `M-<up>` maf-carry-up
   - [X] `M-<down>` maf-carry-down
   - [X] `M-<return>` maf-dup-go (2026-08-20: the copy now lands on
     top of the stack, calc's convention, rather than below the
     source; point travels with it, a mark holding the origin.
     Renamed from maf-dup-below to say so)
   - [X] `M-m` maf-beginning-of-entry
   - [X] `M-f` maf-forward-noun
   - [X] `M-b` maf-backward-noun
   - [X] `S-SPC` maf-forward-space
   - [X] `C-M-u` maf-up-expression
   - [X] `C-M-k` maf-reset
   - [X] `C-M-l` maf-reset-settings
** defined but unbound (M-x only)
   Stack commands with no key in native and no flag route from a bound
   key. Not listed: the ~67 flag-route variants (arcsin under I S,
   vmedian under H u M, identify under I k k, append under H |, ...) —
   those are reachable; entry points (maf-calc, maf-calc-direct),
   module toggles (the m c menu's job), and lib helpers. Generated
   against the sources 2026-08-19.
   - mafcmd-idiv — integer division; its stock \ went to mafcmd-sqrt
   - mafcmd-lud — LU decomposition; its stock v L went to
     mafcmd-flatten
   - mafcmd-arg — complex argument; its stock G went to maf-go-home
   - mafcmd-map, mafcmd-map-stack — explicit mapping commands; the M
     flag is the bound workflow
   - maf-dup, maf-dup-here, maf-clear-selections — split-out halves of
     RET's maf-dup-or-clear-selections
   - maf-edit-add-entry-below — deliberately unbound; C-o one entry
     down covers the gesture (see src/bindings.el)
   - maf-restore-stack — t l runs the choose-a-session variant
     (maf-restore-stack-from)
   - maf-timeline-clear

* profile:vim
  Vim's navigation keys over maf's commands — not vim's command model.
  The profile *derives from native* (2026-08-21: `:derive 'native` in
  src/bindings.el, resolved at every compile — see
  `maf-bindings--effective-defaults'): native's whole layout — the
  mafcmd table, shadowing, additions — flows through with no
  declaration naming vim, and vim's own claims displace whatever they
  overlap, so a motion on j drops the inherited j family whole. Base
  remaps, module keys, and digit entry reach vim on their own paths,
  as they always did. What the motions displace still needs relocated
  homes — families keep their second letters when they move, so they
  relocate whole.
** motions (decided 2026-08-20, implemented in src/bindings.el)
   The split mirrors vim's own: fine motion on h/l, vertical on j/k,
   structural motion on w/b. Each key does what its Emacs cousin does
   — no new motion commands were needed.
   - [X] `h` / `l` — backward-char / forward-char (C-b / C-f). By
     char, not noun: noun-stepping is w/b's job, exactly vim's
     h-versus-w distinction
   - [X] `j` / `k` — next-line / previous-line (C-n / C-p). By line,
     not entry: the earlier by-entry proposal (a maf-entry-down/up
     pair) was passed over for the plain line motion — entries are
     one line in the common case, and line motion also walks the rows
     of a matrix or Big rendering
   - [X] `w` / `b` — maf-forward-noun / maf-backward-noun: the M-f /
     M-b motions on vim's word keys (M-f / M-b stay bound too)
   - [X] `x` — maf-del, C-d's command: vim's delete reflex
     (2026-08-21). The displaced single-key expand moves up a case to
     `X`, whose inherited resident mafcmd-log-exp cedes it (name-only
     in vim); expand's table key `a x` stays too
   - already vim-shaped for free: `G` is maf-go-home in native
   - deliberately no `0` / `$` / `gg`: digits are digit-entry
     starters, $ is auto-algebraic entry, g is reserved for the
     plotting module; M-m stays beginning-of-entry
** relocation table (proposed)
   Everything under a stolen key is relocated or explicitly dropped —
   the compiler checks the accounting. Dropped always leaves M-x, and
   a flag route where one exists.
   - [ ] `j` — the selection/structure family, maf's core, moves to a
     `J` prefix wholesale: J j raise, J i isolate, J e jump-equals,
     J l / J r commute, J D / J x distribute, J M / J f merge, J U
     unpack, calc's selection machinery beneath. A one-letter case
     shift keeps the fingers' map. Displaces mafcmd-conj (J), which
     has no flag route — rehome under a custom prefix (k j or l j,
     the k/l policy's kind of claim) or accept M-x
   - [ ] `l` — maf's custom prefix moves to `L` the same way: L l
     float-frac, L c collect-fractions, L t poly-roots, L f / L F
     factoring, L d / L r angle conversions, L v, L x, L g. Displaces
     mafcmd-ln (L), which keeps its I E flag route and its name;
     calc's log-units family beneath drops to M-x
   - [ ] `k` — the harder twin. Either (a) the family moves to `K`,
     which requires adjusting the keep-args carrier machinery first
     (the free-keys note on K already contemplates exactly this), or
     (b) the calc combinatorics siblings drop to M-x and only maf's
     own rehome: k k evaluate, k s complete-square, k d
     factor-powers. Evaluate is the heavy one and needs a first-class
     home either way — K K if (a); otherwise propose `=` or a
     C-c chord
   - [X] `b` — approved steal (2026-08-20). The binary/financial prefix to `B`: B a / B o / B x
     bitwise, the b-table rows, calc's financial family beneath
     dropping to M-x. Displaces mafcmd-log (B), whose base-10 reading
     stays on H L / I H E; the arbitrary-base log keeps only its name
     — the one real loss in the table, flagged rather than hidden
   - [X] `h` — approved steal (2026-08-20). Calc's help family drops: the global C-h family covers
     describe-*, the m c menu is the discovery surface, and h s
     summary / h t tutorial / h i info stay M-x
   - [X] `w` — approved steal (2026-08-20). calc-why drops: calc-auto-why already volunteers the
     information, M-x calc-why remains (native spends this key
     nowhere either — it is the free-keys list's cheapest key, which
     vim spends on a motion instead)
** phases
   - [ ] 1. settle this section: motions decided and implemented
     (2026-08-20, plain Emacs motions — no new commands needed); the
     relocation table remains, j/k/l the open families and the k
     decision the only structural one
   - [X] 2. declare the motions: the '(vim) delta in src/bindings.el
   - [X] 2b. derivation (2026-08-21): vim registered :derive 'native,
     so the whole native layout flows through and the motions prune
     what they overlap — replacing the interim per-declaration
     vim-skipping (the k/b table filter, the j U retarget).
     tests/bindings-derive.el pins the merge rules and the profile
   - [ ] 3. relocate the displaced families per the table above —
     they are pruned in vim today, reachable by name only
   - [ ] 4. snapshot the result: a vim-profile row sweep beside the
     native one in tests/bindings-snapshot.el, switching profiles in
     the step test
** stolen-key inventory (what each key holds in the native profile today)
   NOTE: do not bind any of this yet — in the skeleton, vim carries
   today's bindings like every profile; the steals and relocations wait
   for the proposals above to settle.
   - `h` — calc-help-prefix (the whole h help family)
   - `j` — the selection prefix, close to maf's core:
     - maf's own: mafcmd-raise (j j), mafcmd-isolate (j i),
       mafcmd-unpack (j U), maf-merge (j M), maf-distribute (j D),
       maf-jump-equals (j e), maf-commute-left/right (j l / j r)
     - calc's selection machinery: calc-select-here/once/more/less/
       next/previous/part, calc-sel-add/sub/mult/div-both-sides,
       calc-sel-isolate/commute/distribute/merge/negate/invert/
       evaluate/expand-formula, calc-rewrite-selection,
       calc-enter/edit/copy/del-selection,
       calc-enable/show/clear/break-selections, calc-unselect, calc-conj
   - `k` — the combinatorics prefix:
     - mafcmd siblings shadowing calc: choose, gcd, lcm, prfac,
       nextprime, totient, moebius, euler, bern, random, shuffle,
       utpt/utpp/utpc
     - maf-only: mafcmd-evaluate (k k), mafcmd-complete-square (k s),
       mafcmd-factor-powers (k d)
     - calc-only leftovers: calc-keep-args (k K), calc-prime-test,
       calc-random-again, calc-extended-gcd, calc-utpn/utpf/utpb
   - `l` — maf's own invented prefix, layered over calc's log-units
     prefix:
     - maf's own: mafcmd-factor-by (l f), mafcmd-factor-gcd (l F),
       mafcmd-collect-fractions (l c), mafcmd-float-frac (l l, the
       float/frac toggle; float and frac stay commands),
       mafcmd-to-degrees (l d), mafcmd-to-radians (l r),
       mafcmd-poly-roots (l t), mafcmd-inverse-function (l v),
       mafcmd-swap-vars (l x), mafcmd-unique-groups (l g)
     - calc's log-units family: calc-lu-plus/minus/times/divide/quant,
       calc-db, calc-np, calc-freq, calc-midi, calc-spn
   - `w` — calc-why (single command)
   - `b` — the binary/financial prefix:
     - mafcmd table rows: and, or, xor, diff, not, clip, lsh, rsh,
       ash, rash, rot, vpack, vunpack, irr, npv, relch
     - calc-only: calc-word-size (b w), calc-percent-change,
       calc-log (b B), calc-lshift/rshift-arith,
       calc-fin-pv/fv/pmt/nper/rate/sln/syd/ddb

* free and weak keys (native)
  Candidate homes for future single-key bindings, generated against
  native's compiled map plus calc's own (2026-08-19). "Weak" = bound to
  something rarely pressed; the cost of taking it is noted. Sorted by
  priority: single letters, then capitals, then punctuation, then
  modified keys.
** single letters
   - `w` — calc-why: its information arrives by itself via
     calc-auto-why, and M-x calc-why remains. The cheapest key in the
     buffer; every other lowercase letter is a live prefix family or a
     real command
   - `y` — calc-copy-to-buffer: niche (yank result into another
     buffer), but no reflex replacement exists. Second choice
   - `g` — calc's graph prefix: weak today, but the plotting module
     (later::plotting in maf.org) will want it. Do not spend
   - reserved, never take: `z` / `Z` (calc's user-definition prefixes),
     `q` (quit is a reflex), `h` (help family)
** capitals
   - none free. `W` was the last one and mafcmd-sqr took it
   - `K` — calc-keep-args: weak-ish, but it is a fancy prefix that
     chains with maf's own M flag (a carrier in maf--map-flag-carriers)
     — take only with that machinery adjusted
   - `Y` — calc reserves it for users; not maf's to spend
** punctuation
   - `{` / `}` — calc-scroll-down/up: C-v / M-v cover scrolling; same
     trade already made when < > took calc's horizontal pair
   - `~` — calc-num-prefix: near-free, but a stretch to reach —
     fine for something occasional, wrong for a repeat-heavy command
   - `:` — calc-fdiv: moderate — fraction division is real but the
     fraction is also typed in digit entry (1 : 3). Cautious
   - not weak, despite looks: `;` (calc-semi, matrix entry punctuation
     — the [ ] lesson)
** modified keys
   - free meta: `M-a` `M-d` `M-e` `M-g` `M-j` `M-q` `M-t` `M-z`,
     and `M-r` since poly-roots moved to l t (2026-08-20; `M-h` went
     to the timeline the same day)
   - cautious meta: `M-v` (scrolling's other half), `M-y` (yank-pop
     pairs with maf-yank's C-y); never `M-x`
   - free-ish control: `C-z` (suspend, commonly rebound anyway); `C-t`
     was one until maf-swap-up took it as a second key (2026-08-20);
     cautious: `C-l`
     (recenter), `C-q` (quoted-insert), `C-r` / `C-s` (isearch over the
     stack is real), `C-j` (calc-over — an RPN staple, weak only for
     algebraic-entry users)
   - never: `C-a/b/e/f/n/p` (cursor motion the stack buffer lives on),
     bare `C-c` (mode-prefix convention — though `C-c C-<letter>` is
     the mode's own slice of it, which is how `C-c C-c` went to the
     simplify), `C-g` `C-u` `C-h` `C-x`

* open decisions
  - [ ] calc profile strictness: strict (only sibling remaps — matches
    the written promise; recommended) vs curated additions on unbound
    keys
  - [X] vim motion semantics (resolved 2026-08-20): h/l by char, j/k
    by line, w/b by noun — each key doing what its Emacs cousin does;
    see profile:vim
  - [ ] vim relocation table for the h/j/k/l/w/b families (proposed in
    profile:vim: j→J and l→L wholesale, b→B, h and w dropped, k the
    open structural decision). h/w/b approved 2026-08-20; what
    remains to decide is j/k/l — the keys holding real families
  - [ ] digit-entry customization surface: per-profile digit declarations
    for users, or a documented API?
  - [ ] custom profiles and module keys: with explicit module targets, a
    custom profile is in no module's list — inherit the clone source's
    module targeting (provenance), or require the profile author to
    declare?
  resolved: module-vs-profile precedence (conflicts are compiler errors,
  so precedence rarely decides anything); profile persistence (customize
  only — maf-persist is a separate concern); user profile seeding for
  the profile's own defaults (declaration-level clone); bindings module
  off keeps one bootstrap key — `m c`, the escape map — because
  re-enabling by M-x alone is too hidden a way back; k and l are
  native's custom-command prefixes (the profile:native policy) — k s
  and k d stay where the fingers know them, and prefix residents are
  evicted lazily, only when a custom command claims the key

* phases — skeleton first
  Build the architecture now; decide no new binding content. The
  skeleton carried today's bindings into all three profiles
  identically; since then calc and vim have been reduced to the
  sibling groups only (see profile:native) — vim equals calc until its
  content phase lays out the navigation keys and relocations.
  - [X] 1. snapshot current behavior in step tests: effective bindings,
    remaps, prefix descendants, module on/off, digit-entry, real
    key-binding answers in a calc buffer (tests/bindings-snapshot.el)
  - [X] 2. registry + compiler: base + three profile maps + user maps +
    compiled suppressions; every profile reproduces today's map exactly
    (behavior, not bytes); conflict validation (core/maf-bindings.el,
    tests/bindings-registry.el; the reproduce-today check lands with
    the phase-3 conversion)
  - [X] 3. one atomic landing: module + digit-map declaration conversion
    (tracked installs), then dispatcher, profile defcustom + switch
    command, live switching, and profile-aware key displays. Not split:
    raw module define-keys under the dispatcher would outrank user maps
    and falsify the lookup order, and a user's first rebind in a user
    map falsifies static key displays
  - [X] 4. document user configuration and migration from direct
    maf-mode-map bindings (the user-configuration and migrating-an-init
    sections above; the live surface is documented in the defcustom,
    mode, and command docstrings)

* later: profile content (out of scope for the skeleton)
  - [ ] audit the remap allowlist and fill the calc profile
  - [X] classify today's native-profile bindings: remap-covered vs
    displacement delta (the profile:native inventory; the shadowing and
    additions groups now target native+vim only)
  - [ ] user-defined profiles (displays land earlier, with the switcher)
  - [ ] vim: motion semantics, relocation table, then the profile
