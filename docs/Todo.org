#+title: Todo

* Todo [0/2]
** Use defmath form
   (maf-defcmd maf-mult (expr arg commit)
   "Multiplication command."
   :arity binary
   :prefix "mult"
   (commit (calcFunc-mul expr arg)))
   => (commit (* expr arg)))  ;; make this work
** TODO Set place/point after committing
** TODO Implement options
   - [ ] simplification
   - [ ] support-mapping
   - [ ] force-line
   - [ ] defcmd may sometimes need the full entry and may also need to write to
     the full entry, not just its expr. How to handle this elegantly?
