#+title: Todo

* Now [0/2]
** NEXT Work on setting point/position after commit for each context target type.
   CASE: target=entry point=home
   => point should stay at home
   CASE: target=subexpr point=subexpr
   => point stays at targeted subexpr
   CASE: target=entry point=line-prefix or EOL of stack entry
   => point stays at either line-prefix or EOL
   CASE: target=equation point=home
   => point should stay at home
   CASE: target=equation point=entry
   3:  sin(y) = k|
   n (mafcmd-neg)
   => This should keep point at the end of the targeted entry, but instead
      point moves to home position.

   - Should the point/position info be stored inside context object?
** NEXT Move core/ files to top-level directory (emacs-maf/core/)

* Later [0/2]
** TODO Set place/point after (maf--commit)
   - If possible, when (eq :target 'subexpr), stick point to the same symbol
     prior to the operation.
** TODO Implement options
   - [ ] simplification
   - [ ] support-mapping
   - [ ] force-line
   - [ ] defcmd may sometimes need the full entry and may also need to write to
     the full entry, not just its expr. How to handle this elegantly?
