#+title: maf

* Guides and Conventions
  A command should take an explicit number of arguments - never implicit.

  For example, (my/calc-factor-by) by default works on the top 2 stack items.
  But for convenience, we may want it to to automatically take the GCD of the
  top stack item if no second argument is given.

  However, this would be a mistake. In order for (my/calc-factor-by) to work in
  context properly, it *cannot* accept an implicit number of arguments.

  Take an example where the point is over a subexpression at stack level m=3.
  The user invokes (my/calc-factor-by) to factor the subexpression. Should the
  subexpression factor by its GCD or by the stack (m=1) item? Both cases are
  valid and useful, but the choice cannot be disambiguated.

  The correct behavior is for the command to either *always* take 2 arguments
  (the target expression and the factor) or *always* take a single argument (the
  target expression - the GCD is automatically selected as the factor).

  If the first behavior is chosen, the `H` flag can be used to *explicitly*
  enable the second behavior. This is the preferred way.

* Docs
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Lisp-Definitions][lisp definitions]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Stack-Access][stack access]]
  [[https://www.gnu.org/software/emacs/manual/html_mono/calc.html#Symbolic-Lisp-Functions][symbolic lisp functions]]

* Plan
  I want to move all of the calc customizations in @my/calc/ into a new package
  called "maf". How should we go about this? I want to make as few changes as
  possible. Just relocate. Some other things:
  - Also create a minor-mode package.
  - This package should be its own git project (how to handle this?)

* Steps
  1) Organize .org file
  2) Develop maf-test
  3) Rewrite main macro
  4) Read calc manual

* Todo [0/23]
  - [ ] Add function to get calc buffer
  - [ ] If on home line, stick point to the "."
  - [ ] Replace noun with digit entry if point is on sub-formula
  - [ ] Remove keep-point flag in macro
  - [ ] Add plus and minus clac functions
  - [ ] Add sin/cos/tan and inverse functions.
  - [ ] Also "stick" point to beginning or end of entry's expression. and also over =, !=, <=/=>
  - [ ] When point is over equation symbol, operate on both sides (unless map is -1).
  - [ ] k k (my/calc-evaluate N) should work on fractions and division.
  - [ ] g i (my/calc-graph-quick-with-range) interval form a:b should work with spaces 'a b'
  - [ ] (calc-fraction ARG) and calc-float should work contextually.
  - [ ] (my/calc-factor-by-gcd) should factor out negative numbers (leave the
    leading term in the binomial positive).
  - [ ] Make (my/calc-sel-negate) contextual.
  - [ ] Should have a form to collect the numerator.
    3:2 sqrt(2) -> 3 sqrt(2) / 2
  - [ ] S-up/down should toggle +/-, ln/e, */, log/^, etc.
  - [ ] Take: y = 1:2 - (5 x - 5 k)^-2 / 2 and represent the negative power as a reciprocal.
  - [ ] (my/calc-quick-variable) should be contextual.
  - [ ] ( and ) should move to next opening/closing paren.
  - [ ] (my/calc-collect-fractions) -> 16:3 x / 1
  - [ ] Line option (calc-option &optional N) should be bound to single key without modifiers.
  - [ ] (my/calc-log-exp-rules) on line should transform entire line.
  - [ ] Binary arity functions should noop on targeting sub-formula at stack=1.
    They have nowhere to take args from.
  - [ ] If command is line-target, then m=2 is the subject and m=1 is the arg,
    same as if point were at home.


* Bugs
  - (my/calc-factor-by-gcd) not working
  - Some commands like * and / don't work in "prefix" area on home line.
    # Found the issue: when you enter 3 * or some other operation, we prevent
    # point from moving so make it easier to operate on lines. However this
    # means that when the digit is entered, point is still on the entry line, so
    # the stack item is being multiplied by itself, then replaced.
  - `saved-point-is-at-home` is nil when it should be true when a binary command
    is invoked at the first column of the home row because (calc-align-stack-window)
    is being repressed.

    This is hard to accommodate for because the macro doesn't run until the
    digit is already pushed to the stack. By that time, point is on line 1
    instead of home.

    Why is point behaving differently in column 1 vs column 2?
  - (my/calc-factor-by-gcd) only works properly on the home line or a duplicated
    value on the home line. It's choosing a different factor on (cplx)-style numbers.
  - (my/calc-commute) doesn't flip the inequality.
    x > 1
    (my/calc-commute)
    1 > x
  - (my/calc-commute) doensn't work properly. It doesn't keep the equation
    balanced.
  - Equal command doesn't work with point on line 1, but works on home.
** BUG: (my/calc-factor-by-gcd) on `x y - 2 x` isn't working.
   BUG: 3 y - 24
** BUG (my/calc-backward-noun) and (my/calc-forward-noun) should ignore line prefix.

* Issues
  - C-g leaves and moves point to home instead of leaving digit entry and retaining point.

* BACKLOG
** Issue: j D (my/calc-sel-distribute)
   Doesn't work contextually. Should work like 'j M'. Work on stack item when at home.
** Instead of having to use the default bindings 'a m &', can you make 'a m o' work?
   This should invert both sides of an equation. Normally the binding would use
   '&' instead of 'o', but 'o' is more convenient and I have that bound to
   'calc-inv' The issue is that the 'a m' sequence expects standard keybindings.
** Flatten conditions
   : Not worth it. Makes the code more complicated actually.
   (my/calc-replace-expr-dwim)
   Flatten the conditions for clarity without duplicating code.

* Refactor
  Context

  The macro is ~192 lines and hard to read because all dispatch logic lives inside deeply nested quasiquotes. The
  compile-time cond on opt-line/opt-map generates different ASTs for different option combinations, whole-line-form
  is a large quasiquoted fragment inlined into two branches, and wrapped-body vs body-for-map are near-identical
  variants. Goal: move all runtime dispatch into a plain Elisp function, shrinking the macro to option-parsing + thin
   glue code.

  ---
  Root causes of complexity

  1. Compile-time branching — opt-line and opt-map are compile-time constants, forcing all context dispatch into
  quasiquote soup rather than a plain cond.
  2. whole-line-form — a 6-line quasiquoted fragment computed at expansion time and spliced into two branches.
  3. wrapped-body vs body-for-map — nearly identical; differ only in whether calc-wrapper and pop-forms are appended.
  4. Equation-map body duplication — body must expand twice with different cl-flet bindings; irreducible, but can be
  isolated cleanly.

  ---
  Approach: extract my/calc-dwim--resolve

  Move all context detection and replacer construction into a new runtime function. The macro becomes a thin shell:
  1. Parses options at compile time (unchanged)
  2. Evaluates runtime inputs into let bindings
  3. Calls my/calc-dwim--resolve to get a context plist
  4. Branches on :mode — emitting body once or twice — using cl-flet to expose replace-expr

  No call-site changes (bindings, options, body syntax all preserved).

* New macro form
  #+BEGIN_SRC emacs-lisp :results silent
  (maf-defcmd my/calc-factor (expr replace)
    (:prefix "fctr" :enable-map -1 :force-line t)
    "Factor the active selection or whole stack entry at point."
    (replace (calcFunc-factor expr)))
  #+END_SRC

* Highlighting
  - in selection mode, highlight the selection
  - highlight both sides of equation separately with different colors

* Major features
  Vim bindings (noun, verb, char)
  Persistent sub-formula highlighting
  Debug modes

* Package
  Binding for "home"
  Args buffer
  Config file with all config settings and default values
   calx: calc with alternative ux
  How to use macro with def math
  Alternative graphing
  Alternative trail. Should support time travel and finding old single entries.
  Use transient mode for prefix commands
  Swap _ and -
  Show all formulas buffer

* Useful calc stuff
** Math
   - `(math-common-constant-factor EXPR)`
     Returns the *constant* factor in `expr`
** Selection, sub-formulas
   - `(calc-find-selected-part)`
     Find the smallest sub-formula containing the cursor. Assumes
     `calc-prepare-selection` has been called first. Returns nil if the cursor
     is not on any part of the formula.
   - `(calc-prepare-selection)`

     Prepare a stack entry for selection operations. `num` specifies which
     entry to use; if omitted, the entry containing the cursor is used.

     Stores results in: `calc-selection-cache-num` (the entry number used),
     `calc-selection-cache-entry` (the entry as a list, as `calc-top-list`
     would return with entry as the selection mode), and
     `calc-selection-cache-comp` (a “tagged” composition mapping cursor
     positions to sub-formulas; see I/O and Formatting Functions).

     A complication: formulas can contain duplicate small integers. In
     `[1, 2, 1]`, both `1`s are `eq` in Lisp, so storing `1` as a selection
     would falsely match both. To prevent this, `calc-prepare-selection`
     replaces plain integers with `(cplx n 0)` lists — displayed identically
     but distinct objects, so no two sub-formulas are `eq`. The next time the
     entry is used in a computation, `calc-normalize` restores them to plain
     numbers.
   - `(calc-encase-atoms)`
     Modifies `x` in-place (via `setcar`) so every sub-formula is a unique
     atom, using the `(cplx n 0)` trick from `calc-prepare-selection`.

* Buffer
  Stack should be normal numbers? Only encased when necessary?
  Are new entries plain or cplx numbers?
  What about cplx numbers?
  Save as atoms
  Debug to print. Show display representation and debug representation.
  How does restore calc stack work? Does it store as atoms or cplx?
  When restored are the cplx numbers still unique?
  Remove m parameter. Always take args from stack. New options dictates how many options.
  Rewrite digit entry mode. No more auto push on nondigit entry.
  Point is 1 branch binary
  Flag areas where you manually edit the stack structure. Need to be very careful
  Is there an inverse to encase atoms that doesn't simplify?
  l F (factor-auto) should prefer negative number in factor
  Full formula buffer should have "recents" section.
  Attach notes to saved formulas
  Tab should be contextual
