# ai-tests

This directory is where AI assistants (Claude or any other) create tests to
verify their own changes.

Use it for scratch/verification tests written during a task — distinct from
`human-tests/`, which holds the interactive tests the user runs by hand (`f4`).
